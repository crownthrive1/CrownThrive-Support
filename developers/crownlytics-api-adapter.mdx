---
title: "CrownLytics API Adapter"
sidebarTitle: "CrownLytics API"
description: "Governed CrownLytics REST API contract for analytics, goals, conversions, statistics and account data with fail-closed credential/runtime controls."
icon: "chart-line"
keywords: ["CrownLytics", "analytics API", "Bearer API", "goals", "conversions"]
---

# CrownLytics API Adapter

<Warning>
  The CrownLytics API credential is restricted runtime-only material and is never published, committed or logged.
</Warning>

## Current state

```yaml
platform_id: ct.platform.crownlytics
service_id: crownlytics
base_url: https://crownlytics.com/api
docs_url: https://crownlytics.com/api-documentation
architecture: REST
response_format: JSON
authentication: Bearer API key
timezone: UTC
credential_ref: vault:crownlytics_api_key
credential_binding: blocked_pending_approved_secret_write
adapter: crownlytics-api-control@1
adapter_scaffold: passed
provider_dispatch: disabled
authenticated_read: open
provider_writes: closed
```

Current first-party docs expose User, Websites, Pageviews, Visitors, Goals, Goal conversions, Annotations, Website statistics, Custom domains, Account payments and Account logs.

THIVEBASE registers eleven strict read-family MCP candidates, all disabled until exact endpoint/method schemas, approved secret binding and authenticated read certification exist.

## Data boundary

Visitor/pageview/session/goal/conversion and account-log data can be personal, pseudonymous or operational depending on configuration. Public metrics must pass CrownThrive claim-definition/effective-period governance; provider numbers do not automatically become approved public claims.

## Source-context anomaly

The founder-supplied capture included a `locticians.com` header immediately before CrownLytics API content. Current first-party docs resolve the API to `crownlytics.com`. Preserve the header as source-context drift; do not merge CrownLytics into Locticians.

## Credential blocker

The founder supplied the current key, but the connected secure-write layer rejected direct Vault insertion before persistence. Planned aliases are `crownlytics_api_key` and `crownlytics_api_key_recovery`.

## Page changelog

- **2026-08-19 v1.0:** added current REST/Bearer/UTC family evidence, fail-closed adapter/MCP state, data/claim boundaries and the preserved Locticians-header anomaly.