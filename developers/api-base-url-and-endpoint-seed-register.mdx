---
title: "API Base URL and Endpoint Seed Register"
sidebarTitle: "API Seed Register"
description: "The initial source-backed register of CrownThrive API hosts, webhook routes, technical endpoint families and documentation references, with verification states that prevent planned contracts from being treated as live."
icon: "link"
---

# API Base URL and Endpoint Seed Register

This is a starting inventory for Phase 2.98. A source-defined host or endpoint is not a production claim. Every item requires environment, authentication, ownership and live-response verification before state advances beyond `source_defined` or `prepared`.

## State vocabulary

- `source_defined` — found in an approved specification or directive;
- `prepared` — route/configuration was designed or reportedly created, but current deployment is not reverified here;
- `public_host_verified` — public website host responds, not necessarily an API;
- `docs_verified` — official documentation is reachable/current;
- `auth_verified` — authentication flow tested;
- `sandbox_verified` — authorized test succeeds;
- `production_verified` — authorized production behavior and owner confirmed;
- `deprecated` — replacement exists;
- `blocked` — cannot currently validate/use;
- `unverified` — known reference, current state unknown.

## Institutional and CHLOM API

### Planned canonical API host

```yaml
api_id: ct.api.institutional.v1
proposed_base_url: https://api.crownthrive.com/v1
state: source_defined
owner_target: CrownThrive IO / Institutional Core
notes: >-
  Treat as proposed canonical namespace until DNS, deployment,
  TLS, auth, health and OpenAPI are verified.
```

### Source-defined CHLOM endpoint families

The July 2026 data/API/security specification defines representative contracts:

#### Identity

- `POST /v1/tenants`
- `POST /v1/organizations`
- `POST /v1/invitations`
- `GET /v1/me`
- `POST /v1/verifications`

#### Registry and rights

- `POST /v1/assets`
- `POST /v1/assets/{id}/versions`
- `POST /v1/evidence`
- `POST /v1/claims`
- `GET /v1/assets/{id}/rights`

#### DLA / LEX

- `POST /v1/dlas`
- `POST /v1/license-templates`
- `POST /v1/offers`
- `POST /v1/rfqs`
- `POST /v1/quotes`
- `POST /v1/agreements`
- `GET /v1/entitlements/{id}`
- `GET /v1/entitlements/verify`

#### Policy and authority

- `POST /v1/policies`
- `POST /v1/policy-evaluations`
- `POST /v1/approvals`
- `POST /v1/holds`
- `POST /v1/governance/proposals`

#### Revenue and settlement

- `POST /v1/usage-events`
- `POST /v1/invoices`
- `POST /v1/payments/callback`
- `POST /v1/settlements`
- `POST /v1/settlements/provider-webhooks`
- `GET /v1/reconciliation`

#### Remedies and audit

- `POST /v1/disputes`
- `POST /v1/disputes/{id}/events`
- `POST /v1/disputes/{id}/evidence`
- `POST /v1/remedies`
- `POST /v1/appeals`
- `GET /v1/audit/events`

#### Integrations and public verification

- `POST /v1/webhooks`
- `POST /v1/webhooks/endpoints`
- `GET /v1/event-deliveries`
- `POST /v1/event-deliveries/{id}/replay`
- `POST /v1/imports`
- `GET /v1/verify/{fingerprint}`
- `GET /v1/catalog`
- `GET /v1/licenses/{id}/status`

All remain `source_defined` until implemented and tested. Phase 3 may refine paths while preserving semantic IDs and migration history.

## Virality Music

```yaml
api_id: ct.api.virality.stripe-webhook
host: https://vm.crownthrive.com
path: /api/stripe/webhook
full_url: https://vm.crownthrive.com/api/stripe/webhook
state: prepared
interface_type: webhook
provider: Stripe
owner_target: Virality Music commerce
notes: >-
  Previously prepared for checkout completion, delayed payment,
  expiration, refunds and disputes. Reverify deployment, event list,
  signature secret, endpoint ownership and last delivery before activation.
```

Current Pass D platform state:

```yaml
platform_id: ct.platform.virality-music
public_host: https://vm.crownthrive.com
public_host_state: public_host_verified
application_repository: not_connected
public_api_docs: not_discovered
public_api_base_url: unverified
stripe_webhook_reference_state: source_defined_or_prepared_not_reverified
```

Additional Virality API domains to discover: catalog/search, worlds/canon/characters, books/readers, tracks/releases, Backroad FM, products/orders/entitlements, rights/readiness/licenses/inquiries, commissions, sponsorship inventory, adaptation intake, proof/metrics, assets/files, corrections and admin/ops. Candidate public and operator tools are defined in the Virality Licensing and Rights Register, but no tool or endpoint advances beyond `source_defined` until repository/data-source/auth/side-effect tests pass.

## KJV Visualized / Sermon Toolkit

```yaml
platform_hosts:
  - https://kjvsermontoolkit.crownthrive.com
  - https://sermontoolkit.crownthrive.com
state:
  public_hosts: public_host_verified
  editable_source_repository: blocked_http_500
  public_api_docs: not_discovered
  public_api_base_url: unverified
known_webhook_paths_from_release_evidence:
  - path: /api/stripe-webhook
    state: enabled_endpoint_observed
    certification: unverified
  - path: /api/connect/webhook
    state: enabled_endpoint_observed
    certification: unverified
protected_source_revision: aa0e1b659f7a20452aa84f163b71cdfc59b48ada
```

Endpoint existence and a historical successful payment do not certify order creation, idempotent fulfillment, rights-ID/license generation, entitlement issuance, protected delivery, refund/dispute revocation or customer-account ownership. Discover actual current contracts for Scripture, reader state, devotionals, rooms, products, organizations, sermon intake/transformations, entitlements, webhooks, partner products and support only after the original source repository is repaired and integrity-checked. Do not infer API availability from developer navigation or ordinary web routes.

## AdLuxe

Phase 2.98 Pass D separates CrownThrive's platform/business layer from provider capability:

```yaml
platform_id: ct.platform.adluxe-network
platform_surfaces:
  - ct.platform.adluxe-network
  - ct.platform.adluxe-studio
  - ct.platform.adluxe-places
public_host_reference: https://adluxe.crownthrive.com
application_repository: not_connected
current_deployment_revision: unverified
references:
  - url: https://adluxe.crownthrive.com/docs/stats-api
    classification: crownthrive_documentation_reference
    state: unverified
  - provider: Adserver.Online
    classification: vendor_saas_ad_serving
    official_capability_state: docs_verified
    documented_families:
      - management
      - reporting
      - decisioning
      - javascript
      - statistical_reporting
    crownthrive_account_plan: unverified
    crownthrive_api_entitlement: unverified
    base_urls: unverified
    auth_scopes: unverified
    rate_limits: unverified
    production_test: not_run
```

The first adapter must be authenticated read/report only: advertiser/publisher/property/zone/campaign/creative/delivery/conversion/report/balance retrieval and CrownLytics reconciliation. Production trafficking, pause/resume, budget changes, credits, earnings and payouts remain gated until exact owner/account/auth/contracts, rollback, budget and audit controls pass. Provider documentation never advances the CrownThrive deployment to `production_verified`.

## Phase 2.98 Pass D3 creator, mentorship and event platforms

### CrownFluence

```yaml
platform_id: ct.platform.crownfluence
public_content: previously_retrieved
current_domain: unverified_in_this_pass
application_repository: not_connected
engine_provider_account: unverified
api_base_url: unverified
public_api_docs: not_discovered
current_creator_campaign_data: unverified
current_economic_state: unverified
```

Candidate domains include creators/profiles, brands/campaigns, briefs, applications, assignments, deliverables, releases/rights, approvals/disclosures, publication, performance, compensation, disputes and corrections. A public page or recovered blackpaper title does not prove current provider/API access, audience-equity economics or active creator rights.

### ThrivePeer

```yaml
platform_id: ct.platform.thrivepeer
public_content: previously_retrieved
current_domain: unverified_in_this_pass
application_repository: not_connected
engine_provider_account: unverified
api_base_url: unverified
public_api_docs: not_discovered
current_program_match_data: unverified
privacy_safety_contracts: unverified
```

Candidate domains include programs/cohorts, mentor/participant profiles, verification, matching, availability, sessions/check-ins, goals/resources, consent/privacy, safety/moderation, payments/rewards, feedback and corrections. No API may represent self-described expertise as verified licensure or expose private mentoring records.

### ThriveGather

```yaml
platform_id: ct.platform.thrivegather
recovered_provider_lineage: GoBrunch
current_provider_account_plan: unverified
current_workspaces_rooms_urls: unverified
application_repository: not_connected
api_base_url: unverified
public_api_docs: not_discovered
recording_export_state: unverified
```

Candidate domains include workspaces, rooms, events, access tiers, memberships/entitlements, invites, hosts/moderators, capacity, sponsors, attendance, recordings/assets, embeds, regions/time zones, analytics and incidents. Recovered workspace URLs, secret keys or provider instructions must never be copied into public docs without current authorization and verification.

### ThriveTickets

```yaml
platform_id: ct.platform.thrivetickets
public_content: previously_retrieved
current_domain: unverified_in_this_pass
application_repository: not_connected
engine_provider_account: unverified
api_base_url: unverified
public_api_docs: not_discovered
processor_order_ticket_scan_state: unverified
```

Candidate domains include events, organizers, venues, schedules, ticket types, inventory, prices/fees, codes, orders/payments, attendees/tickets, transfers, scans/admission, waitlists, refunds/disputes, organizer settlements, waivers/releases, accessibility and incidents. Payment success, QR possession and scan activity remain separate evidence states.

## Phase 2.98 Pass D7 education and community platforms

### ThriveArena

```yaml
platform_id: ct.platform.thrivearena
current_domain: unverified
application_repository: not_connected
engine_provider_account: unverified
api_base_url: unverified
public_api_docs: not_discovered
challenge_scoring_reward_data: unverified
```

Candidate domains include challenges/versions, organizations, eligibility, submissions/attempts, scoring rules, reviewers, leaderboards, anti-cheating, appeals, accommodations and CrownRewards evidence. No endpoint may convert score/rank/points into professional licensure, employment, equity or institutional authority by default.

### QuizNuggets

```yaml
platform_id: ct.platform.quiznuggets
current_domain: source_or_registry_lineage_only
application_repository: not_connected
engine_provider_account: unverified
api_base_url: unverified
public_api_docs: not_discovered
quiz_question_attempt_export: unverified
ai_review_policy: unverified
```

Candidate domains include quizzes, question banks, question versions, sources/explanations, authors/reviewers, AI-assistance, embeds, competitions, attempts/scores, moderation and corrections. High-consequence assessment content requires a verified human-review policy before publication or credential use.

### CrownConsults

```yaml
platform_id: ct.platform.crownconsults
current_domain: unverified
application_repository: not_connected
engine_booking_payment_provider: unverified
api_base_url: unverified
public_api_docs: not_discovered
expert_roster_qualification_state: unverified
```

Candidate domains include expert/provider profiles, qualifications/licenses, service categories/scopes, availability, engagements/offers/prices, bookings/payments, deliverables/completion, refunds/disputes, privacy/retention and support. Self-described expertise, a CrownThrive badge or mentor/alumni status is not a regulated professional credential.

### ThriveAlumni

```yaml
platform_id: ct.platform.thrivealumni
recovered_implementation_lineage: Zaialumni_plus_Donation_Addon
current_provider_version_account: unverified
application_repository: not_connected
api_base_url: unverified
public_api_docs: not_discovered
member_chapter_committee_data: unverified
fundraising_payment_state: unverified
```

Candidate domains include alumni members, originating programs/cohorts, eligibility, chapters, committees, roles/terms, service/volunteer work, fundraising/contributions, moderation, elections/appointments where adopted, corrections and support. Historical civic/government titles cannot become current corporate/fiduciary authority through an API import.

### CrownThrive Commons

```yaml
platform_id: ct.platform.crownthrive-commons
current_engine_provider_version: unverified
application_repository: not_connected
api_base_url: unverified
public_api_docs: not_discovered
auth_account_linking: unverified
profile_group_message_export: unverified
```

Candidate domains include local profiles, CrownThrive ID account links, groups/memberships, relationships, messages, announcements, visibility, moderation, reports, suspensions, appeals, retention, export/delete and incidents. Commons is not CrownThrive ID and cannot create cross-platform roles or consent by implication.

### Digital Business Academy

```yaml
platform_id: ct.platform.digital-business-academy
disposition: reserve_predecessor
relationship_to_crownthriveu: founder_adjudicated_predecessor_reserve
current_domain_engine_repository_data: unverified
api_base_url: blocked_pending_adjudication
public_api_docs: not_discovered
```

Do not design or publish an active API until the asset is adjudicated as a predecessor, separate school/imprint, reserve, dormant platform or future CrownThriveU specialization. Any later implementation must include migration, canonical, credential, learner-data and article/changelog effects.

## ThriveTools OPT

Founder-supplied CrownThrive developer evidence on 2026-08-19 establishes a distinct ThriveTools OPT API application and displayed product version `v4.0.0`.

```yaml
platform_id: ct.platform.thrivetools-opt
parent_family: ct.platform.thrivetools
service_id: thrivetools_opt
host: https://optimize.thrivetools.io
base_url: https://optimize.thrivetools.io/api
observed_product_version: v4.0.0
authentication:
  header: Authorization
  credential: server_side_runtime_only
credential_storage:
  provider: Supabase Vault
  primary_and_recovery_aliases: present
  fingerprints_match: true
adapter:
  slug: thrivetools-opt-api-control
  version: 1
  verify_jwt: true
  provider_dispatch: disabled_pending_method_certification
state:
  docs: founder_source_verified
  credential_vault_binding: passed
  parent_child_identity: passed
  http_methods: pending_source_verification
  authenticated_read: open
  provider_writes: closed
```

Documented exact endpoint URLs:

- `/get-images` — optimized-image collection;
- `/get-media` — media-image collection;
- `/upload` — file upload/optimization with `images[]`, `compression_ratio`, and `media_upload`;
- `/delete` — array `image_id` plus `permanent_delete`;
- `/get-single-image` — one `image_id`;
- `/check-status` — optimization/storage quota status.

The source shows `Content-type: application/json` for Delete and Get Single Image and `Content-type: form/data` for Upload. It does **not explicitly show HTTP verbs**, so no verb is promoted from inference. The first live certification should be a bounded D0 `check-status` request after exact method verification. Upload and Delete remain D2 write/destructive surfaces and are not enabled in the institutional adapter.

The source-visible Check Status example shows `maximum_optimize: Unlimited`, `available_optimize: Unlimited` and `current_storage: 20GB`. Preserve that as supplied documentation/account evidence rather than a fresh live quota certification.

See [ThriveTools OPT API Adapter](/developers/thrivetools-opt-api-adapter).

## Collab Portal Secure API

Phase 2.99 now has a verified OpenAPI 3.0.3 contract for the SuitePro-powered Collab Portal Secure API.

```yaml
platform_id: ct.platform.collab-portal
api_id: ct.api.collab-portal.secure
swagger_url: https://portal.crownthrive.com/secure-api/swagger
base_url: https://portal.crownthrive.com/secure-api/
openapi_version: 3.0.3
snapshot_sha256: 65a59d29859e0ba04993985c7e3bdfcd1d4e07b97f0a17cd9d1683eea6423376
auth:
  type: paired_api_key_headers
  public_header: X-Public-ID
  secret_header: X-Secret-Key
state:
  docs: docs_verified
  base_url: docs_verified
  auth_contract: docs_verified
  secure_runtime: active_v2_vault_capable
  vault_binding_path: verified
  screenshot_transcription_auth_attempt: failed_401
  screenshot_transcription_interpretation: not_character_exact_do_not_treat_as_provider_key_invalid
  production_credential_binding: pending_exact_secure_copy
  authenticated_read: pending_exact_secure_copy
  authenticated_write: not_attempted_write_gate_closed
  webhook_openapi_registration_contract: not_exposed
  webhook_ui_contract: verified_s102
  webhook_project_events:
    - Project Created
    - Project Updated
    - Project Deleted
  webhook_sender_integrity: pending
  webhook_delivery_retry_replay: pending
  webhook_receiver_certification: pending
```

Verified endpoint families:

- contacts: meta/list/find/create/update;
- companies: meta/list/find/create/update;
- projects: meta/list/find/update;
- marketing: subscribe contacts to audiences;
- Worlds: list Worlds when the Power-up is available.

For the institutional PM loop, the supported Secure API contract is intentionally narrower: discover project meta, list/find an existing project, update only approved project-status/custom fields, then read back and verify. The Secure API does not expose project creation, tasks, comments, milestones, files, signatures or webhook registration/event schemas in this specification. Those negative findings remain scoped to S101. Separate `S102` UI evidence now verifies configurable webhook endpoints and Project Created/Updated/Deleted event selection, but it does not prove sender authentication/signing, delivery-attempt identity, retry/replay behavior, timeout/dead-letter semantics or a certified inbound receiver. Keep those layers independently versioned rather than collapsing UI capability into API or production certification.

The OpenAPI uses scope-like strings inside `apiKey` security requirements. They are not OAuth scopes, and some labels are internally inconsistent. Do not derive least-privilege authorization from those annotations alone; verify actual key permissions and observed behavior.

The portal UI inspection showed a dated 20,000-request monthly account limit, while the generic OpenAPI `Error-429` example mentions 200. Treat the account UI as the newer account-specific observation and the schema example as stale/generic until provider/account evidence reconciles it.

See [Collab Portal Secure API Adapter](/developers/collab-portal-secure-api-adapter).

## CrownLytics

Phase 2.98 verified the current public host and official API documentation.

```yaml
api_id: ct.api.crownlytics.public
host: https://crownlytics.com
base_pattern: https://crownlytics.com/api/{endpoint}
docs_url: https://crownlytics.com/api-documentation
architecture: REST
response_format: JSON
auth_scheme: bearer_api_key
timezone: UTC
state:
  public_host: public_host_verified
  docs: docs_verified
  auth: unverified
  sandbox: unverified
  production_request: unverified
```

Documented families include User, Websites, Pageviews, Visitors, Goals, Goal conversions, Annotations, Website statistics, Custom domains, Account payments and Account logs. A current public example uses `/api/statistics/{website_id}`. Exact methods/paths, schemas, rate limits, plan requirements and side effects still require extraction.

## CrownPulse

Phase 2.98 verified the public CrownPulse host and a current Bearer-key API example.

```yaml
api_id: ct.api.crownpulse.public
host: https://crownpulse.com
known_path: /api/campaigns
known_method: POST
auth_scheme: bearer_api_key
state:
  public_host: public_host_verified
  public_example: verified
  full_docs_extraction: pending
  auth: unverified
  sandbox: unverified
  production_request: unverified
```

Current public API families include Campaigns, Campaign notifications, Notification handlers and Custom domains. Full API documentation, concrete schemas, delivery side effects, rate limits, consent/suppression behavior and provider integrations still require verification.

## Phase 2.99 current API reconciliation — August 19, 2026

### ThrivePush

`ct.platform.thrivepush` / service `thrivepush` now has current REST/JSON/Bearer/UTC documentation, 18 API families, a JWT-protected `thrivepush-api-control@1` scaffold, 13 disabled MCP family contracts and a checksum-controlled service-worker asset. Credential binding is blocked pending an approved secret-write path; authenticated reads are open; writes are closed. See [ThrivePush API Adapter](/developers/thrivepush-api-adapter).

### CrownLytics

`ct.platform.crownlytics` / service `crownlytics` now has current REST/JSON/Bearer/UTC documentation for 11 API families, a JWT-protected `crownlytics-api-control@1` scaffold and 11 disabled MCP read-family contracts. Credential binding is blocked; authenticated reads remain open; writes are closed. The copied `locticians.com` header is preserved as source-context drift rather than identity evidence. See [CrownLytics API Adapter](/developers/crownlytics-api-adapter).

### CrownPulse Admin

`ct.platform.crownpulse` / service `crownpulse_admin` now has founder-observed privileged REST/Bearer Admin API evidence for Users, Plans, Payments and Domains. `crownpulse-admin-api-control@1` is JWT protected; 14 MCP contracts are disabled, six with human-approval requirements. The UI reports update available `v61.0.0` and support expired; installed version remains unverified. Credential binding is blocked; privileged mutations are closed. See [CrownPulse Admin API Adapter](/developers/crownpulse-admin-api-adapter).

The connected secure-write layer rejected direct Vault insertion for the founder-supplied keys before persistence. No raw secret is represented as stored. Provider dispatch remains fail-closed.

## CrownThrive IO and domains

Phase 2.98 verified the current public CrownThrive IO host and API documentation.

```yaml
api_id: ct.api.crownthrive-io.public
host: https://crownthrive.io
base_pattern: https://crownthrive.io/api/{endpoint}
docs_url: https://crownthrive.io/api-documentation
architecture: REST
response_format: JSON
auth_scheme: bearer_api_key
timezone: UTC
state:
  public_host: public_host_verified
  docs: docs_verified
  auth: unverified
  sandbox: unverified
  production_request: unverified
```

The official API documentation exposes endpoint families for User, Links, Link statistics, Projects, Pixels, Splash pages, QR codes, Data, Notification handlers, Custom domains, My teams, Team members, Teams member and Account payments. Phase 2.98 must still extract exact methods/paths, parameters, response schemas, quotas/rate limits, plan requirements and side effects before SDK/MCP generation.

Do not publish or test a user API key without authorized credential handling. Documentation verification does not by itself advance any endpoint to `production_verified`.

Other relevant hosts that require inspection include `crownthrive.dev`, `crownthrivesupport.com`, `thriverelay.com`, `opsoasis.com`, `thrivemaps.com`, `thrivekiosks.com`, `adluxeplaces.com` and `adluxestudio.com`. Domain ownership is not proof of deployed API capability.

## Phase 2.98 public API discovery update — August 17, 2026

The public-domain sweep verified visible Bearer-key REST documentation for four CrownThrive services while preserving authentication/sandbox/production as separate states.

### CrownThrive IO

`https://crownthrive.io/api/{endpoint}` · REST/JSON · Bearer API key. Families include User, Links/statistics, Projects, Pixels, Splash pages, QR codes, Data, Notification handlers, Custom domains, teams and account payments/logs. Observed example: `POST /api/links`.

### ThrivePush

`https://thrivepush.io/api/{endpoint}` · Bearer API key. Families include Websites, Subscribers/stats/logs, Campaigns, Personal notifications, RSS automations, Recurring campaigns, Flows, Segments, handlers, domains, teams and account payments/logs. Observed example: `POST /api/personal-notifications`.

### CrownLytics

`https://crownlytics.com/api/{endpoint}` · Bearer API key. Families include Websites, Pageviews, Visitors, Goals/conversions, Annotations, Website statistics, domains and account payments/logs. Observed statistics route family: `/api/statistics/{website_id}`.

### CrownPulse

`https://crownpulse.com/api/{endpoint}` · Bearer API key. Families include Statistics, Campaigns, Campaign notifications, handlers, domains, teams and account payments/logs. Observed example: `POST /api/campaigns`.

### Locticians

```yaml
platform_id: ct.platform.locticians
host: https://locticians.com
public_host: verified
engine_vendor: unverified
public_api_docs: not_discovered
api_base_url: unverified
webhooks: unverified
export_contract: unverified
```

Current public product operation is verified, but Phase 2.98 has not established the current directory engine/vendor/repository/database from authoritative evidence. Do not infer a vendor from templates, page structure or generic legal language.

Priority future adapter families: profiles/providers, categories, locations/service areas, verification records, profile claims, Get Matched requests, reviews, membership/entitlements, Nexus Credits, community/events, ThriveSeat links and CrownRewards participation.

### ThriveSeat

Current public enterprise positioning advertises API integration access, but equivalent public endpoint documentation was not recovered in this scan.

Phase 2.98 Pass C1 now adds implementation lineage:

```yaml
platform_id: ct.platform.thriveseat
host: https://thriveseat.com
public_host: verified
canonical_platform: ThriveSeat
engine_lineage: Frezka SaaS Laravel
vendor: Iqonic Design / CodeCanyon lineage
vendor_relationship_evidence: Envato update notices + Iqonic ThriveSeat support ticket
installed_server_version: unverified
installed_mobile_version: unverified
repository: unverified
database: unverified
public_api_claim: capability_claim
public_api_docs: not_discovered
api_base_url: unverified
auth: unverified
webhooks: unverified
vendor_backend_api_structure: verified_from_Frezka_docs
```

The current public ThriveSeat homepage still exposes `Frezka Saas` wording. That is white-label implementation/branding leakage, not the institutional platform name.

Official Frezka SaaS documentation shows a Laravel backend containing API-controller structure and current vendor releases continue to be maintained, but **vendor API architecture is not a public CrownThrive API contract**. Inspect the installed ThriveSeat routes, tenant boundaries, authentication, webhooks and side effects before generating SDK/MCP tools.

A July 29, 2026 cPanel message records a ThriveSeat account suspension; the current public site is reachable, so treat it as a historical incident requiring restoration/root-cause evidence rather than current `suspended` state.

### Media and ministry

Current KJV Visualized/Sermon Toolkit hosts are reachable, but no public API docs were discovered. CrownThrive Studios and Melanated TV public hosts were retrieved, but no public CrownThrive-native API docs were discovered.

Phase 2.98 Pass B9 advances several **provider** interfaces separately from CrownThrive-native APIs:

#### Radio.co

```yaml
provider_api: Radio.co Public API
public_base_v1: https://public.radio.co/stations/{station_id}/
public_base_v2: https://public.radio.co/api/v2/{station_id}
docs_state: verified
crownthrive_station_id: unverified
crownthrive_test_state: not_run
```

Verified vendor families include station status, history, current/next track, requests and schedule. Apps/Studio API documentation families also exist in the current developer portal. Recent Backroad/legacy Virality station operation is provider-evidenced, but station ID/account API entitlements remain separate.

#### Riverside

```yaml
provider_api: Riverside Business API
current_version: v3
base_family: /api/v3/
auth: Bearer API key
availability: select Business accounts
crownthrive_account_evidence: Pro trial 2026-08-12
crownthrive_business_api_entitlement: unverified
```

Do not generate a CrownThrive Riverside adapter until Business API eligibility is verified. Current official docs say v1/v2 are deprecated; isolated older endpoint pages require reconciliation before implementation.

#### Viloud

```yaml
provider_api: Viloud Delivery API
base: https://deliver.viloud.tv/channel/{channel_id}
docs_state: verified
operations: [channel, schedule, epg]
management_api: documented_as_forthcoming
crownthrive_channel_ids: unverified
```

Do not infer dashboard/channel mutations from the Delivery API.

#### Streamit Laravel

Current official vendor docs identify Streamit Laravel web **v2.2.0 (2026-06-09)** and describe Short Drama configuration API endpoints. This is vendor capability evidence only; Melanated TV's installed version, local routes, repo, database and add-on state remain unverified.

#### Go Flipbooks

CrownThrive's institutional reader role and upload contracts are verified, but no current public Go Flipbooks API/developer contract was discovered. Keep `current_engine/vendor/api = unverified`.

Detailed provider state is maintained in **Media Provider Version, API and Exit Registry**. Detailed domain observations remain in **Phase 2.98 Domain and API Discovery Ledger**.

**Fleet rule:** `scanner-unresolved` ≠ offline; `docs_verified` ≠ production\_verified; `not_discovered` ≠ nonexistent.

## Required verification packet

For every seed item record:

1. DNS/TLS/host owner;
2. environment;
3. service/provider/account;
4. official docs/schema;
5. auth and scopes;
6. authorized request/response test;
7. health/version;
8. webhook signature/retry where applicable;
9. data classification;
10. support/incident path;
11. source repository/deployment;
12. last verified timestamp;
13. deprecation/replacement;
14. DAIL event or evidence reference.

## Security rule

Do not publish private admin/internal base URLs, client secrets, webhook secrets, tokens, database hosts or debug endpoints in public documentation. Public API references must be intentionally approved.