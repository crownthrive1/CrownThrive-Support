# {{CANONICAL_NAME}} Framework Skill

Package: `{{PACKAGE_ID}}`  
Framework: `{{FRAMEWORK_ID}}`  
Agent: `{{FRAMEWORK_AGENT_ID}}`

## Purpose

Use this skill when a request falls inside the governed scope of {{CANONICAL_NAME}}. The skill may interpret the public framework contract, run package validation, invoke approved tools, prepare evidence, and call the governed API/MCP/private-runtime path where authorized.

## Authority

- Non-voting by default.
- D3 is human-reserved.
- Agent D remains the parent certification gate.
- Framework/package/workflow/tool success never creates sovereign authority.
- Missing evidence, private-runtime availability, rights, security or required approval means HOLD.

## Protected implementation boundary

Public package material may expose safe schemas, thresholds, interfaces, hard-block contracts and digests. Do not reproduce private calibration, proprietary weights, private eval corpora, defensive rules, credentials, restricted evidence or private economic/policy logic.

## Standard execution

1. Resolve the current package manifest and framework doctrine.
2. Validate the package with `python3 scripts/validate_child_repository.py`.
3. Use `python3 engine/engine.py health` for local package health.
4. Use only tools declared in `tools/tools.v1.json`.
5. Use only API/MCP operations declared in the package contracts.
6. Route protected/material decisions through the governed private runtime.
7. Preserve evidence, rollback and reopen triggers.

## Commercial boundary

The package may describe candidate services, implementation kits, reference packages, training and managed-runtime scopes. It must not invent prices, enable checkout, grant licenses, claim certification or create customer entitlements without separate authority.
