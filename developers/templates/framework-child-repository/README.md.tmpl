# {{CANONICAL_NAME}}

This is the governed CrownThrive framework execution package for `{{FRAMEWORK_ID}}`.

## Institutional status

- Package identity: `{{PACKAGE_ID}}`
- Framework agent: `{{FRAMEWORK_AGENT_ID}}`
- Parent control repository: `{{PARENT_REPOSITORY}}`
- Parent packet PR: `{{PARENT_PR}}`
- Package state: `{{PACKAGE_STATE}}`
- Runtime integration allowed by the current factory packet: `{{RUNTIME_INTEGRATION_ALLOWED}}`
- Optional standalone repository projection: `{{OPTIONAL_REPOSITORY_PROJECTION}}`
- Physical repository required: **false**
- Sovereign vote eligibility: **false**
- D3 authority: **human-reserved**
- Public/production activation at scaffold creation: **false**

The framework child is the package itself: workflow + validator + engine + skill + tools + API/MCP contracts + evals + federation contract + commercialization manifest. A separate GitHub repository is optional distribution packaging and is not evidence of framework existence, acceptance, certification or sovereign authority.

## Required package chain

```text
accepted source/doctrine
→ package manifest
→ validator
→ engine + decision interface
→ skills + tools
→ evals/negative cases
→ API/MCP contracts
→ protected runtime/Vault binding where required
→ trusted GitHub OIDC runtime context
→ Agent D parent certification
→ governed framework acceptance
→ controlled public/managed distribution when separately authorized
```

## Protected algorithm boundary

Public-safe package code may expose schemas, dimensions, thresholds, hard-block interfaces, deterministic validators and digests. CrownThrive proprietary calibration, weights, private evaluation corpora, defensive rules, restricted evidence, credentials and protected economic/policy logic stay in the approved private runtime/Vault and are invoked through the governed federation/API/MCP path. Missing required private runtime or digest means HOLD.

## Development

Run:

```bash
python3 scripts/validate_child_repository.py
python3 engine/engine.py health
```

All material changes use reviewed CrownThrive governance. This package cannot self-certify, self-activate, change quorum, remove Agent D, create D3 authority, create a sovereign vote, invent a price/license/checkout, or waive an upstream phase/security/rights/legal/finance gate.
