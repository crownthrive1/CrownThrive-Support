---
title: "Virality SoundCloud and Catalog Optimization Adapter"
sidebarTitle: "Virality SoundCloud Adapter"
description: "Governed public SoundCloud embeds, non-API metric provenance, CrownLytics evidence, internal Virality catalog tooling, and shadow recommendation boundaries."
icon: "plug"
keywords: ["SoundCloud", "Virality Music", "non-API evidence", "MCP", "adapter", "embeds", "recommendations"]
---

# Virality SoundCloud and Catalog Optimization Adapter

## Status

**Service state:** documented<br />**Public SoundCloud embed standard:** passed for supported oEmbed/HTML5 Widget use<br />**Public embed credential requirement:** none<br />**SoundCloud API use:** `REMOVED_BY_FOUNDER_OVERRIDE`<br />**SoundCloud API credential state:** `NOT_APPLICABLE` — credentials must not be requested, stored, configured, certified, or used<br />**Authenticated SoundCloud provider-read certification:** `NOT_APPLICABLE`<br />**Provider write gate:** closed<br />**Promotion execution:** not implemented<br />**Public-document posture:** contracts and safeguards only; no secrets or private object references

### Founder override — August 20, 2026

Kavonte Jones Sr. directed that CrownThrive and Virality Music **will not use SoundCloud APIs**. This supersedes prior packet language that treated authenticated SoundCloud API access as a future certification path.

This adapter therefore separates:

1. **public presentation/playback** through SoundCloud-supported public oEmbed/iframe/HTML5 Widget surfaces; and
2. **non-API metric/evidence ingestion** from separately governed, provenance-backed sources such as permitted exports, founder-supplied reports, or public observations classified at the evidence strength actually proved.

No absent SoundCloud API credential is a blocker because SoundCloud API credentials are no longer part of the architecture. A functioning public embed remains playback evidence only and never upgrades a metric observation to a stronger evidence class.

## Service boundaries

- Supabase holds the private operational registry and dated evidence rows.
- Virality supplies stable work identity and approved public projection.
- SoundCloud supplies the supported public playback/embed surface only; CrownThrive does not use SoundCloud APIs. Metric evidence must arrive through separately governed non-API sources with explicit provenance and evidence classification.
- CrownLytics supplies definitions and recommendation evaluation.
- the Website Surface & Interaction Agent plus its Virality visual-QA child govern public presentation evidence;
- GSO and CHLOM remain outside the adapter's authority but provide mandatory readiness gates.

## Public embed contract

For public Virality Music listening pages, the default is the supported SoundCloud embed stack:

- **simple public playback:** use SoundCloud oEmbed / the standard iframe widget;
- **custom parent-page playback controls or player events:** use the official HTML5 Widget API around the standard iframe;
- **metrics/evidence:** do not use the public widget as a substitute for stronger evidence; route metrics through the governed non-API evidence lane with provenance and evidence-state labeling.

SoundCloud's oEmbed response is designed for responsive embedding and returns widget markup at width `100%`; `auto_play` defaults to `false`. The HTML5 Widget API is the supported JavaScript control surface for playback methods and events.

CrownThrive requirements:

- responsive width within the governed page container;
- autoplay off by default;
- descriptive iframe/player title;
- preserve canonical SoundCloud attribution/source route where appropriate;
- surrounding controls remain keyboard operable with visible focus;
- preserve click-to-load/consent behavior when the current privacy model requires user-initiated third-party media;
- reserve player space to reduce avoidable layout shift;
- lazy-load below-fold embeds where the supported implementation permits;
- no SoundCloud API token, client secret or provider credential in page source, iframe parameters, public JS, GitHub or Mintlify;
- a player failure must not destroy the page's core copy, rights route or business CTA.

Official provider references:

- [SoundCloud oEmbed](https://developers.soundcloud.com/docs/oembed)
- [SoundCloud HTML5 Widget API](https://developers.soundcloud.com/docs/api/html5-widget)

Implementation across every applicable Virality route remains a separate Sites/public-surface verification task. The standard itself does not claim that every current route has already been migrated.

## Proposed MCP surface

Virality internal/catalog MCP tools remain separately governed. **No SoundCloud API/MCP tool is authorized or planned under the current founder override.** The public SoundCloud widget does not require an MCP tool.

| Tool | Risk | Intended behavior | Current state |
| --- | --- | --- | --- |
| `virality.assets.list` | D0 | bounded public-safe or service-role catalog list | disabled |
| `virality.assets.get` | D0 | one identifier, explicit field projection | disabled |
| `virality.stats.current` | D0 | latest dated snapshot at its proved evidence class | disabled |
| `virality.stats.history` | D0 | cursor-paginated evidence history | disabled |
| `virality.recommendations.list` | D1 | shadow recommendations only | disabled |
| `virality.recommendations.decide` | D2 | human approval or rejection event | disabled |
| `soundcloud.sync.daily` | D1 | retired authenticated SoundCloud API read identifier | `REMOVED_BY_FOUNDER_OVERRIDE` |
| `virality.promotion.execute` | D3 | consequential public mutation | not implemented |

The retired `soundcloud.sync.daily` identifier is retained only as a drift-detection marker. Agents must not implement, invoke, credential, schedule, or certify it.

## Read contract

Every read tool must enforce:

- stable identifiers;
- explicit allowed fields;
- service or user scope;
- bounded page size and cursor pagination;
- request budget and timeout;
- evidence date and evidence state;
- deterministic ordering;
- no private file identifiers, credentials, raw provider payloads or customer records;
- audit receipt with request class, actor, tool version and result count.

## Daily non-API evidence contract

The daily SoundCloud lane is an observation/provenance workflow, not an authenticated API synchronization job. Each run is idempotent on source class, scheduled date and evidence reference. One run records the source class, evidence date, provenance reference/digest where permitted, evidence state, completion state and any validation error. A snapshot remains unique by date, metric family and governed source identity.

Public HTML, an iframe, oEmbed output or a public profile observation must never be represented as authenticated provider evidence. Public observations remain `observed_unverified` unless another permitted non-API source independently supports a stronger governed classification.

Permitted non-API evidence sources may include:

1. founder-supplied or otherwise authorized reports/screenshots with provenance;
2. permitted account exports or reports that do not require CrownThrive to integrate the SoundCloud API;
3. public observations captured with date, source URL/surface and method;
4. previously accepted evidence retained as a dated baseline.

If a material metric cannot be supported by a permitted non-API source, record `NEED_TO_DO`, `HOLD`, `unknown`, or the applicable evidence state. Do not reintroduce a SoundCloud API requirement to close the gap.

Official public SoundCloud widgets remain independently allowed under the public-embed contract.

## Recommendation write contract

A recommendation request includes asset ID, target surface, action type, score, evidence window, evidence digest, reasons, safeguards, expiry and algorithm version. It must fail closed when rights, release, canon, audience, commerce, freshness, cooldown, balance or rollback evidence is absent.

Approval creates an append-only decision event. It does not execute a placement. Execution requires a separate tool, separate scope, separate approval, target-specific preflight and target-specific rollback.

## Public-surface and visual QA handoff

Any proposed change to public SoundCloud presentation is handed to `ct.subagent.virality-public-surface-visual-qa` under the Website Surface & Interaction Agent. The child checks responsive fit, player overlap, keyboard/focus behavior, visual hierarchy, third-party loading, accessibility, layout stability and Virality brand coherence.

A metric/evidence observation cannot substitute for visual QA. A successful visual QA cannot upgrade metric provenance or waive any non-API evidence requirement.

## Security and rollback

- No anonymous or authenticated end-user role can access the private control schema.
- Operational tables use RLS and service-role-only policies.
- The private storage bucket has no public listing or object-read policy.
- Credentials belong in a managed secret store, not GitHub, Mintlify, logs, prompts or database metadata.
- Public SoundCloud embeds never contain CrownThrive provider credentials.
- Rollback first disables the affected schedule/tool or restores the last accepted Sites/public-surface version as applicable. Evidence rows are preserved for audit.

## Related contracts

- [Public Surface Visual Quality & Media Embed Standard](/standards/public-surface-visual-quality-and-media-embed)
- [Virality Public Surface Visual QA Subagent](/automation/virality-public-surface-visual-qa-subagent)
- [Website Surface & Interaction Agent](/automation/website-surface-interaction-agent)
- [Platform API Adapter Matrix](/developers/platform-api-adapter-matrix)
- [Virality Asset Catalog and Metrics Registry](/platforms/virality-asset-catalog-and-metrics-registry)
- [CrownLytics](/platforms/crownlytics-institutional-registry)
- [Daily Runbook](/runbooks/virality-daily-metrics-and-placement-optimization)