---
title: "CHLOM Wallet Smart-Account Testnet Readiness"
sidebarTitle: "Smart-Account Readiness"
description: "Controlled-test readiness contract for CHLOM Wallet ERC-4337 v0.9 audit lineage, reproducible runtime identity, multi-provider Sepolia bytecode observation, WebAuthn gates and non-broadcast deployment preparation."
icon: "diagram-project"
---

# CHLOM Wallet smart-account testnet readiness

**State:** `HOLD_PREDEPLOYMENT`  
**Target chain:** Sepolia / `eip155:11155111`  
**External bytecode observation:** complete through two providers  
**Exact release runtime reproduction:** complete  
**Official audit extraction:** complete  
**Runtime codehash independent approval:** pending  
**Simulation and broadcast:** disabled

The next CHLOM Wallet blockchain step remains evidence and review—not token issuance, account deployment, custody, or a transaction.

## Evidence now complete

### Official release and source identity

```yaml
repository: eth-infinitism/account-abstraction
release_tag: v0.9.0
release_commit_sha: b36a1ed52ae00da6f8a4c8d50181e2877e4fa410
release_tree_sha: f75688b14e23ebddb9b81011b036bab15de89089
entrypoint_source_blob_sha1: 3b4feb6ec0ed1cf41310b24858c81f5012572af9
release_entrypoint: 0x433709009B8330FDa32311DF1C2AFA402eD8D009
```

The stale tagged deployment artifact still records the earlier v0.8 address. That historical source divergence remains documented rather than deleted.

### Official Cantina audit

The exact 498,502-byte audit PDF is pinned by Git blob and SHA-256. Bounded extraction resolves the initial and holistic review commits and preserves the report's finding summary. The holistic follow-up `EntryPoint.sol` blob is identical to the release `EntryPoint.sol` blob.

See [EntryPoint Audit & Code-Identity Review](/developers/chlom-wallet-entrypoint-audit-review).

### Reproducible release runtime

The exact release commit, locked dependency tree, compiler `0.8.28`, Cancun EVM target, optimizer runs `1,000,000`, `viaIR`, and release salt were used to deterministically deploy EntryPoint on an ephemeral Hardhat chain configured with Sepolia chain ID.

```yaml
result: PASS_ERC4337_V09_REPRODUCIBLE_RUNTIME_MATCH_HOLD_INDEPENDENT_APPROVAL
workflow_run_id: 32569154292
entrypoint_address: 0x433709009b8330fda32311df1c2afa402ed8d009
runtime_code_bytes: 22425
runtime_codehash: 0x280d5c7c0de94b512401eb9c4b0ef0436275ff03627aad0ce1f93ab1627187a0
matches_observed_sepolia_runtime: true
external_rpc_used: false
```

### Multi-provider Sepolia observation

Two public providers independently returned the same chain ID, address, byte length, and runtime codehash using only `eth_chainId` and `eth_getCode`.

```yaml
provider_count: 2
provider_agreement: true
runtime_code_bytes: 22425
runtime_codehash: 0x280d5c7c0de94b512401eb9c4b0ef0436275ff03627aad0ce1f93ab1627187a0
```

Provider availability loss is treated as an availability HOLD, not bytecode disagreement. Any successful refresh provider must match the pinned evidence.

## What remains unresolved

The evidence now demonstrates:

1. exact upstream release source identity;
2. exact audit artifact identity and bounded extraction;
3. audited-source lineage to the release EntryPoint source;
4. deterministic release address reproduction;
5. deterministic release runtime reproduction under Sepolia chain semantics;
6. equality between reproduced runtime and observed Sepolia runtime;
7. prior two-provider external bytecode agreement.

The Wallet Agent is still the originator and cannot independently approve its own code-identity conclusion. The runtime codehash therefore remains:

```yaml
runtime_codehash_independently_approved: false
runtime_codehash_verified_for_deployment: false
disposition: HOLD_INDEPENDENT_CODE_IDENTITY_REVIEW_REQUIRED
```

## Assigned review path

```yaml
request_id: ct.wallet.review.entrypoint-runtime-codehash.sepolia.v1
work_id: ct.work.chlom-wallet.erc4337-runtime-codehash-review.v1
protocol_verifier: ct.chlom.agent.blockchain-crypto
security_verifier: ct.chlom.agent.security
quorum_gatekeeper: ct.relay.agent-d
recovery_authority: ct.chlom.agent.recovery
construction_gates: [CG-049, CG-056]
```

The requested approval is limited to continuing testnet predeployment. It cannot authorize account deployment or broadcast.

## Remaining hard gates

1. Independent runtime-codehash approval.
2. Smart-account implementation selection and review.
3. Factory/deterministic deployment selection and review.
4. Passkey-validator selection and review.
5. Authenticated browser WebAuthn canary.
6. Production recovery and attestation policy.
7. Reviewed account call data and init code.
8. Bundler compatibility and UserOperation simulation.
9. Independent wallet/account security review.
10. Testnet signer and recovery-authority receipt.
11. Rollback and recovery drill.
12. Separate governed testnet broadcast authorization.

## Non-execution boundary

```yaml
account_implementation_selected: false
factory_selected: false
passkey_validator_selected: false
funded_signer_authorized: false
user_operation_created: false
simulation_completed: false
deployment_authorized: false
broadcast_authorized: false
custody: false
money_movement: false
production_rights_grant: false
phase_advancement: false
```

No public testnet or mainnet transaction is authorized by this page.
