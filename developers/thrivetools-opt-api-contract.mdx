---
title: "ThriveTools OPT API Contract"
sidebarTitle: "ThriveTools OPT API"
description: "Public-safe developer contract for the currently evidenced ThriveTools OPT v4.0.0 API, its REST base, endpoint risk classes, fail-closed controls and remaining certification gates."
icon: "compress"
keywords: ["ThriveTools OPT", "image optimization", "API", "optimize.thrivetools.io", "check-status", "Phase 2.99"]
---

# ThriveTools OPT API Contract

## Identity

```yaml
parent_platform: ct.platform.thrivetools
capability_id: ct.capability.thrivetools.opt
name: ThriveTools OPT
version_evidence: 4.0.0
public_host: https://optimize.thrivetools.io
rest_base: https://optimize.thrivetools.io/api
authentication: API key via Authorization header
credential_values_public: false
```

This page is a public-safe projection of current governed evidence. It does not expose API keys, credential fingerprints, runtime secret paths, private defensive logic, internal conformance fixtures or failure evidence.

## Certification state

| Control | State |
| --- | --- |
| API documentation | `pass` |
| Primary server-side credential binding | `pass` |
| Authenticated `GET /check-status` | `pass` |
| Recovery/continuity reference | `open` |
| Remaining D0 endpoint-method and bounded-response certification | `open` |
| Control-plane request-body bounding | `open` |
| MCP adapter exposure | `open` |
| Upload/delete provider writes | `closed` |

## Endpoint contract

| Method | Route | Class | Governed state |
| --- | --- | --- | --- |
| `GET` | `/get-images` | D0 read | documented; dispatch gated pending bounded certification |
| `GET` | `/get-media` | D0 read | documented; dispatch gated pending bounded certification |
| `POST` | `/get-single-image` | D0 read-style request | documented; dispatch gated pending bounded certification |
| `GET` | `/check-status` | D0 read | authenticated live read verified |
| `POST` | `/upload` | D2 write | closed / disabled |
| `POST` | `/delete` | D2 write | closed / disabled |

## Runtime boundary

The current archive records a JWT-protected `thrivetools-opt-api-control` v1 adapter deployed fail-closed for provider dispatch. Authentication success must be evaluated against the expected API success schema; HTTP status alone is not sufficient proof.

No upload or delete action is authorized by this documentation.

## Ecosystem relationship

ThriveTools OPT is a child capability of `ct.platform.thrivetools`, not a separate top-level platform. Its host and API records remain separately addressable so health, version, permissions, events, rights, pricing and deployment evidence can evolve without contaminating the parent or ThriveTools SEO state.

See the [ThriveTools Institutional Registry](/platforms/thrivetools-institutional-registry) for parent/child and domain-manifest reconciliation.