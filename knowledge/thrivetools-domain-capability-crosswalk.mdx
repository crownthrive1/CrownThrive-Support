---
title: "ThriveTools Domain and Capability Crosswalk"
sidebarTitle: "ThriveTools Crosswalk"
description: "Machine-readable reconciliation of the ThriveTools parent family, SEO and OPT child capabilities, S100 root domain and later recovered subdomain manifest without corrupting historical source counts."
icon: "diagram-project"
keywords: ["ThriveTools", "domain crosswalk", "SEO", "OPT", "thrivetools.io", "S100", "subdomains", "Phase 2.99"]
---

# ThriveTools Domain and Capability Crosswalk

## Canonical graph

| Institutional identity | Type | Parent | Host | Source-count treatment |
| --- | --- | --- | --- | --- |
| `ct.platform.thrivetools` | parent platform/tool family | — | `thrivetools.io` | existing S100 root-domain/portfolio identity |
| `ct.capability.thrivetools.seo` | child capability | `ct.platform.thrivetools` | `seo.thrivetools.io` | descendant host from later subdomain manifest; no top-level platform increment |
| `ct.capability.thrivetools.opt` | child capability | `ct.platform.thrivetools` | `optimize.thrivetools.io` | descendant host from later subdomain manifest; no top-level platform increment |

## Count preservation

The S100 Holdings domain universe and the later master-link/subdomain universe are separate source snapshots. Reconciliation must preserve both:

```yaml
s100_root_record:
  id: S100-DOM-057
  domain: thrivetools.io
  mapped_property: ThriveTools

later_subdomain_edges:
  - seo.thrivetools.io
  - optimize.thrivetools.io

mutate_s100_82_row_count: false
promote_children_to_top_level_platforms: false
```

This prevents a later, more detailed manifest from retroactively changing what the S100 `82 domains` figure meant.

## Source-label preservation

A recovered CrownThrive platform/brand inventory separately names `ThriveTools`, `ThriveTools SEO` and `ThriveTools OPT`. Current institutional classification does not delete those labels. It changes their relationship semantics from ambiguous peer-like source labels to a parent plus two governed children.

## Verification boundary

- root ThriveTools identity: resolved;
- SEO child identity/host: resolved; runtime/API state open;
- OPT child identity/host: resolved; current API evidence recovered; remaining runtime/MCP certification open;
- registrar/DNS/DNSSEC/TLS/renewal truth: independent current-state verification and not inferred from a link manifest.