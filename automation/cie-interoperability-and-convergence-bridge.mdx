---
title: "CIE interoperability, CHLOM pallet, and Convergent bridge"
description: "The controlled-test parent anchor for CIE interoperability, protected-runtime boundaries, CHLOM pallet packaging, and research-only continuity to Convergent Ecosystem."
---

# CIE interoperability, CHLOM pallet, and Convergent bridge

<Callout type="warning">
This page documents a **candidate controlled-test packet**. CIE remains `PROVISIONED_UNLINKED`, non-operational, and non-voting. The CHLOM pallet is unregistered, Convergent Ecosystem remains research-only, and Phase 3 remains blocked.
</Callout>

## Exact packet identity

| Surface | Exact state |
|---|---|
| Canonical parent base | `c7f14b73cff09f00a8f94f15a8587289de18ff7b` |
| Parent candidate | CrownThrive-Support PR `#246`, draft |
| Child base | `073da74bb6eb1fde31b9a6d0321bb85baf5ac8fd` |
| Child candidate PR | `crownthrive1/CrownThrive-CIE#6` |
| Current child candidate head | `33a218b6ea05feed1c9a22dd5d3f07c36407fec4` |
| Previous child candidate head | `ce2344a72fb0c4ca699c538b9c10593007c65517` |
| Current parent reanchor | `developers/manifests/cie-interoperability-reanchor.v2.json` |
| Historical predecessor manifest | `developers/manifests/cie-interoperability.v1.json` |
| Interoperability digest scheme | `sha256-canonical-json-sortkeys-v1` |
| Candidate semantic contract digest | `e337defbe44d74f6c050528cb4fc21c0f20b577f8ceb2480e501479ffca990a3` |
| Protected CIE public contract digest | `e5e6ac0e9cf6749ba361435bb65ad212f78562960d0b5522898e06583b8d86c2` |
| Child exact-head governance | `PASS_VALIDATION_ONLY`, run `32594128955`, 16 tests |
| Parent certifier | `ct.relay.agent-d` |
| Verified OIDC authority receipts observed | `0` |

The v1 manifest is preserved as predecessor evidence. The v2 reanchor is the only current-child-head projection. The child records parent main, PR, branch, and manifest identity but deliberately does not pin the mutable parent proposal head; the parent binds the child’s exact head. This asymmetric model prevents a circular SHA dependency.

## Agentic machine

`ct.framework-agent.cie` is the dedicated CIE parent agent. It has a D2 ceiling, no sovereign vote, no D3 authority, and no self-certification or self-activation authority. Six specialized CIE subagents remain prospective and non-voting. They may prepare bounded work but cannot independently verify Agent-C-authored bytes.

The child machine exposes contracts for health, evaluation, public-safe explanation, CHLOM pallet packaging, and Convergent preflight. Only the local D0 package-health contract is present; every runtime-facing API/MCP operation remains disabled.

## Decision envelope and ethics

The public-safe decision envelope accepts evidence references and digests, consent, rights, provenance, and bounded context. It returns `PASS_CANDIDATE`, `HOLD`, or `BLOCK`, non-numeric dimension bands, public-safe explanations, and a runtime receipt.

Public-safe dimensions are identity fit, story alignment, community value, brand safety, and legacy impact. Numeric weights and calibration are not public. Hard blocks include sensitive-trait or group-identity inference, demographic targeting, manipulative engagement, harassment or discrimination, unclear provenance, missing or withdrawn consent, uncleared rights, missing appeal controls, private-evidence exfiltration, and protected-runtime digest mismatch.

A `PASS_CANDIDATE` is advisory. It is not certification, entitlement, legal approval, operational authorization, or a sovereign vote.

## Protected runtime and IP

The registered protected algorithm is `ct.algorithm.cie.v1`. Its implementation body, calibration, weights, private evaluation corpora, defensive rules, restricted evidence, credentials, fingerprints, and private economic or policy controls remain in Vault/private runtime.

Only the algorithm identifier, version observation, public contract digest, schemas, safe dimensions, and hard-block interfaces are projected here. Missing runtime evidence, unavailable runtime, or digest mismatch returns `HOLD`.

Publication classification is `PUBLIC_STANDARD_CANDIDATE`, with publication on hold pending Issue #131 and independent IP/Rights/Licensing review.

## CHLOM pallet

The proposed pallet is `ct.pallet.cie-interoperability`, with candidate module ID `ct.module.chlom.cie-interoperability`.

Its current state is `CANDIDATE_UNREGISTERED`. It accepts sanitized references and digests only, emits public-safe decision envelopes only, and has no direct protected-runtime access. This packet does not create a Supabase module, policy, grant, edge function, MCP registration, API deployment, or database mutation.

Registration requires the exact parent/child relationship and digest, verified OIDC workflow/ref/environment/agent/capability binding, Agent-D parent certification, accepted Issue #131 disposition, independent Security/Privacy and Operations/SRE evidence, RLS/grant and negative-access tests, rollback/readback proof, and constitutional quorum.

## Continuity to Convergent Ecosystem

Convergent Ecosystem remains `RESEARCH_CANDIDATE`. It may eventually accept only a sanitized CIE decision envelope. It cannot receive protected CIE implementation material or direct protected-runtime access.

Promotion is blocked until CIE is `LINKED_GOVERNED`, Agent D certifies the parent relationship, the private runtime is verified for the exact CIE head, the CHLOM pallet is registered and certified, and a separate Convergent packet is accepted.

## Commercial candidate surfaces

Candidate offers may package the licensed public-standard reference, a managed private runtime service, governed API/MCP access, implementation and integration services, operator training, and conformance assessment design.

No exact price, Stripe Product/Price, checkout, license grant, certification claim, or customer entitlement is active.

## Security and operations gates

Both observed parent and child default branches lack branch protection. Verified OIDC authority receipts remain zero. Pull-request validation used read-only contents permission and received no OIDC authority. The trusted federation-runtime job was correctly skipped on the child pull request.

The connected Supabase security-advisor perimeter remains open for independent review. This packet does not modify RLS, grants, or security-definer RPC permissions.

## Tests, rollback, and reopen

The child exact-head workflow compiled both engines, ran package and negative-case validators, executed all 16 unit tests, checked package health, and rejected whitespace/conflict-marker drift. Its PASS is validation evidence only and has no certification or vote effect.

The parent validator enforces exact child head, lineage, digest, lifecycle, authority, IP, CHLOM, Convergent, security, test evidence, asymmetric binding, and commercial fail-closed states.

Rollback reverts candidate commits and restores the prior reanchor without erasing evidence history. Reopen on parent-main or child-head movement, digest change, runtime mismatch, invalid OIDC binding, Agent-D block/deny, specialist HOLD/FAIL, IP reclassification, or commercial-claim change.
