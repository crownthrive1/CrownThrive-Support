---
title: "ThriveSupport Institutional Registry"
sidebarTitle: "ThriveSupport Registry"
description: "Phase 2.98 registry for CrownThrive institutional knowledge, covering legacy Help Center, Mintlify projection, crownthrivesupport.com routing and policy memory."
icon: "book-open"
keywords: ["ThriveSupport", "CrownThrive Support", "Mintlify", "Help Center", "crownthrivesupport.com", "institutional knowledge", "Phase 2.98"]
---

# ThriveSupport Institutional Registry

## Record identity

- Record class: `platform-intelligence`
- Platform/framework family: `ThriveSupport / Help Center Knowledge Architecture / CrownThrive Support`
- Program phase: `2.98`
- Inherited institutional release: `2.7.9.1`
- Extraction pass: `A — institutional core and public control plane`
- Evidence state: `multi-era / partially verified-current`
- Primary function: institutional memory, support knowledge, public policy/rule projection, operational documentation, recovery and machine-readable knowledge publication

## Canonical role

Current CrownThrive public material describes ThriveSupport as the institution's written memory and source-of-truth structure. It is explicitly framed as more than a FAQ system: a place for platform rules, governance posture, corridor/integration rules, technical runbooks, operating procedures, security/incident posture, versioned frameworks and public documentation.

Public source evidence also ties ThriveSupport to Collab Portal, Governance Stack, CHLOM, InfoSec and the corridor system.

This role is consistent with the merged `2.7.9.1` institutional architecture, but the implementation must remain layered. A public documentation deployment is not itself the complete private institutional authority.

## Lineage model

### Layer 1 — historical Help Center

Observed host: `help.crownthrive.com`

Phase 2.99 Workstream 3 re-verification separates two current facts. Search-index snapshots still expose a large legacy Help Center corpus with categories including Legal and Compliance and General Policies, plus article titles/summaries across privacy, CCPA, data deletion, breach preparedness, payment policy, affiliate terms, intellectual property, accessibility, sustainability, dispute resolution and platform-specific terms. Direct live retrieval of the legacy host now presents a SimpleBase access/sign-in gate.

State: `partial-indexed-recovery / live-auth-required / source-recovery-corpus`

Rules:

- preserve it as historical source evidence while extraction continues;
- do not automatically treat every article as current policy;
- retain article title, URL, category, source date where available, recovery state and successor mapping;
- distinguish educational legal/compliance articles from binding current agreements;
- do not destroy it until dependency, redirect and archival coverage are proven.

### Layer 2 — CrownThrive Support / Mintlify institutional projection

Repository: `crownthrive1/CrownThrive-Support`

Current role after merged release `2.7.9.1`:

- public-safe institutional knowledge projection;
- governance and standards library;
- CHLOM public-safe institutional record;
- platform intelligence;
- Legal Depot architecture;
- API/MCP/developer standards;
- runbooks and workflows;
- correction/history records;
- phase/release records;
- machine-readiness specifications.

State: `active-working-public-projection`

This repository/Mintlify deployment is **not** authorized as storage for credentials, raw private evidence, customer data, confidential contracts, secret CHLOM internals or unrestricted private operational state.

### Layer 3 — crownthrivesupport.com ThriveSupport public application

Current CrownThrive portfolio material names `https://crownthrivesupport.com` as the primary hub for Help Center Knowledge Architecture / ThriveSupport. Workstream 3 now verifies that the host is serving a distinct public ThriveSupport application with a substantial category tree but sparse article population.

Current evidence therefore upgrades only the **public shell/reachability** state:

- public HTTP content and application routing: `verified`;
- category/taxonomy surface: `verified_dated_snapshot`;
- substantive article completeness: `sparse / incomplete`;
- canonical-source authority: `no` — it remains a projection;
- authenticated/private knowledge enforcement: `unverified`;
- API/source repository/provider/export/backup state: `unverified`;
- terminology/canonical drift: `present`, including `Cultural Impact Engine (CIE)` versus canonical **Cultural Imprint Engine** and older ThriveAlumni civic-governance framing.

Future authenticated/private areas require real server-side/application authorization, not hidden navigation or robots rules. The branded application must consume the same governed knowledge authority rather than become a competing source of truth.

State: `public-shell-live / sparse-content / reconciliation-required / private-auth-unverified`

### Layer 4 — `docs.crownthrive.io` governed documentation target

Phase 2.98/2.99 documentation-governance decisions register `docs.crownthrive.io` as the target canonical documentation host, independently of the current branded ThriveSupport application. This remains a **target host**, not a current deployment claim.

Workstream 8 must decide the durable routing relationship among:

- `docs.crownthrive.io` — governed public/developer institutional documentation target;
- `crownthrivesupport.com` — branded public/authenticated support entry;
- current Mintlify projection — replaceable documentation service/source projection;
- `help.crownthrive.com` — legacy SimpleBase recovery/archive/access lane.

No host may become a competing institutional source of truth merely because it is public or older.

State: `target-host / routing-ADR-required`

## Phase 2.99 Workstream 3A projection IDs

The multi-projection estate now has stable machine identities:

- `ct.projection.support.legacy-simplebase` → `help.crownthrive.com` legacy recovery/archive lane;
- `ct.projection.support.mintlify-working` → connected GitHub/Mintlify working public-safe projection;
- `ct.projection.support.thrivesupport-public` → live `crownthrivesupport.com` public support shell;
- `ct.projection.support.docs-target` → target `docs.crownthrive.io` governed documentation host;
- `ct.authority.knowledge.private-core` → target private structured registry/policy/evidence authority.

These IDs are independent from hostnames and vendors so routing, provider or deployment changes do not destroy lineage. The [Relationship, Portfolio & Namespace Crosswalk](/knowledge/phase-2-99-workstream-3a-relationship-portfolio-crosswalk) is the current machine overlay.

### Platform family versus current documentation implementation

```yaml
platform_family_id: ct.platform.thrivesupport
current_documentation_platform_id: ct.platform.crownthrive-support
relationship: projection_implementation_of
repository_id: repo.crownthrive.support
documentation_service_id: ct.service.documentation.mintlify
conflict_record: CT-CON-036
```

`ct.platform.thrivesupport` is the institutional knowledge/support platform family and routing doctrine. `ct.platform.crownthrive-support` is the current connected documentation-platform implementation/projection used by the GitHub/Mintlify workflow. The latter does not replace ThriveSupport as an institutional family and neither replaces `ct.authority.knowledge.private-core`. Holdings row `S100-PORT-010` is therefore a typed projection relationship, not an alias decision.

## Knowledge authority model

The institutional authority is not defined as “whatever page is most recent.” It is:

```text
authoritative structured registries
+ approved source evidence
+ effective-dated policies
+ version/correction history
+ role/authority decisions
        ↓
audience-specific projections
        ├── public Mintlify / CrownThrive Support
        ├── future crownthrivesupport.com public support
        ├── authenticated member/operator knowledge
        ├── restricted evidence views
        └── machine APIs/MCP resources
```

## Public/private boundary

### Public-safe

- platform explanations;
- public claims with evidence/as-of state;
- published policies;
- support articles;
- public runbooks where disclosure is safe;
- public developer/API contracts;
- current framework summaries;
- release notes and correction history;
- public status and lifecycle notices.

### Authenticated/relationship-scoped

- member-specific procedures;
- customer/partner/developer guidance;
- organization/team operating instructions;
- entitlement-dependent documentation;
- approved internal-but-nonsecret procedures.

### Restricted/strategic/machine-only

- private contracts;
- raw rights evidence;
- customer/identity/payment records;
- credentials and secret references;
- security-sensitive runbooks;
- investigations;
- private economics;
- unpublished works/source files;
- confidential CHLOM methods;
- policy-engine internals;
- raw authorization state;
- private evaluation artifacts.

No public repository or public documentation host may become the storage authority for those classes.

## Search and retrieval control

ThriveSupport search must become identity- and classification-aware.

Required order:

1. identify caller and assurance level;
2. determine role, relationship, tenant/platform and purpose;
3. apply resource/data classification policy;
4. select permitted search/index partitions;
5. retrieve only authorized material;
6. record access/evidence event where required;
7. generate answer/projection within the same authorization boundary.

Retrieving restricted material first and attempting to redact it after model generation is not an acceptable primary control.

## Automated publication model

```text
source / registry / policy change
→ stable-ID resolution
→ source-authority and version check
→ audience/data classification
→ secrets/PII/restricted-content scan
→ transformation to projection
→ legal/rights/cultural/claim rules
→ link/schema/accessibility/SEO tests
→ approval gate where required
→ publish
→ search/sitemap/index refresh
→ DAIL/change event
→ drift monitor
→ rollback path
```

Goal: routine knowledge publication should become highly automated while consequential legal, rights, cultural, security and authority decisions remain gated.

## Legacy Help Center extraction obligations

Phase 2.98 must enumerate at minimum:

- every category;
- every article title and route;
- recovery class;
- likely policy/support/SOP/platform/framework ownership;
- publication/last-updated evidence where available;
- current/superseded/educational/unknown disposition;
- successor page or canonical record;
- legal-binding versus informational classification;
- public/private suitability;
- duplicate and contradictory content;
- migration/redirect requirement.

## Legal Depot interaction

Legacy Help Center Legal and Compliance content may contain useful historical policy evidence but must not override a newer effective-dated agreement merely because it remains searchable.

Legal policy precedence requires:

- stable policy ID;
- policy family;
- version;
- effective date;
- actor/platform/geography/transaction applicability;
- acceptance mechanism;
- supersedes/superseded-by relationship;
- counsel-review state;
- source evidence;
- public projection.

## Support routing model

Target routing should separate:

- `support case` — account, billing, technical, access or customer problem;
- `knowledge` — how-to, rules, explanations and self-service;
- `legal/policy` — effective agreements and compliance notices;
- `status/incident` — outages and operational status;
- `roadmap/feedback` — ThriveMaps/product-direction lane;
- `execution/collaboration` — Collab Portal;
- `developer` — API/MCP/SDK contracts;
- `restricted institutional` — authorized private control plane.

This prevents ThriveSupport from becoming an undifferentiated catch-all.

## Domain and deployment verification queue

Before `crownthrivesupport.com` receives current-production status, verify:

- domain ownership and registrar control;
- DNS record and target;
- TLS certificate;
- canonical host behavior;
- redirect behavior from historical support hosts;
- application/deployment provider;
- authentication capability;
- authorization enforcement;
- cache/CDN behavior for private content;
- robots and sitemap behavior;
- analytics/monitoring;
- backup/export;
- incident rollback;
- vendor exit.

## MCP/API opportunity

Future support/knowledge MCP tools should not merely expose raw search. Candidate governed capabilities include:

- `search_public_knowledge`
- `search_authorized_knowledge`
- `get_current_policy`
- `get_policy_history`
- `get_platform_record`
- `get_runbook`
- `get_correction_history`
- `create_support_case`
- `get_case_status`
- `submit_knowledge_feedback`
- `get_release_state`

Each tool requires audience, scope, side-effect class, audit behavior and protocol-version pinning.

## Phase 2.99 handoff

Required machine records:

- `knowledge_record`
- `source_record`
- `projection_record`
- `policy_record`
- `article_lineage`
- `category_taxonomy`
- `audience_class`
- `data_class`
- `role`
- `authorization_policy`
- `search_index`
- `publication_job`
- `redirect`
- `support_route`
- `mcp_tool`
- `audit_event`
- `retention_rule`
- `backup_record`

## Current conclusion

ThriveSupport is verified as a current CrownThrive institutional doctrine and knowledge-governance framework, but the knowledge estate currently has **multiple projections that must be reconciled rather than merged by assumption**:

1. `help.crownthrive.com` — legacy SimpleBase corpus: `partial-indexed-recovery / live-auth-required`;
2. `crownthrive1/CrownThrive-Support` \+ Mintlify — active working public-safe institutional projection;
3. `crownthrivesupport.com` — live ThriveSupport public shell with sparse content and terminology drift;
4. `docs.crownthrive.io` — target governed documentation host, not yet verified deployed.

The private structured registries/evidence/policy authority remains distinct from all four projections. Workstream 0 must retry indexed/authenticated legacy recovery before reconstruction; Workstream 3 must preserve article/source/projection lineage; Workstream 8 must finalize the public/authenticated routing ADR and policy publication model.

Current state: `multi-projection / public-shells-verified-in-part / canonical-authority-separated / reconciliation-required`. Phase 3 remains blocked until the support/articleization hard-exit requirements are satisfied.