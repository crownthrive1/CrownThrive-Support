---
title: "Collab Portal, Access and Signatures"
sidebarTitle: "Collab & Signatures"
description: "Controlled partner workspace for NDAs, briefs, approvals, files, milestones, e-signatures, versioning, audit logs, and sensitive-IP access at CrownThrive."
icon: "file-signature"
---

# Collab Portal, Access and Signatures

The **Collab Portal OS** is the controlled execution workspace for partners, contributors, vendors, licensees, clients and selected operators. It complements public support and GitHub/Mintlify by handling relationship-specific documents, approvals and work that should not be globally public.

## Core functions

The portal should support:

- party and organization identity;
- NDA and agreement intake;
- scoped project/workspace access;
- briefs, deliverables and milestone acceptance;
- secure file exchange;
- version history;
- comments and structured approvals;
- digital/electronic signatures where legally appropriate;
- controlled access to sensitive IP;
- license and entitlement references;
- invoice/payment references;
- audit logs and export;
- offboarding and access revocation.

## Signature doctrine

A click, approval event, typed signature, third-party e-signature and cryptographic signature can have different legal and evidentiary properties. The system should record the signature method, document version, signer identity, authority, timestamp, relevant consent and verification evidence.

## Access control

Workspace access follows least privilege and expires when the relationship or project ends unless a continuing role is explicitly approved. Sensitive source material should not be copied into general support or agent context merely because a collaborator has access to it.

## Relationship to CHLOM

The Collab Portal captures execution evidence and approvals. CHLOM records the resulting rights, licenses, restrictions, authority and durable evidence references where relevant.

## Current automation-integration state

The Collab Portal **Secure API contract is now verified** from the live OpenAPI 3.0.3 documentation and supplied specification snapshot. The canonical API server is `https://portal.crownthrive.com/secure-api/`, authenticated with server-side `X-Public-ID` and `X-Secret-Key` headers.

The verified project surface currently supports:

- `GET /project/meta` — account-specific project field schema;
- `GET /projects` — project inventory;
- `GET /project/{type}/{identifier}` — lookup by UID, exact name, substring or most-recent;
- `PUT /project/{type}/{identifier}` — update an existing project.

The Secure API contract does **not** currently expose project creation, task CRUD, comments, milestones, file uploads, signatures or webhook registration/event schemas. That negative finding remains authoritative **for the Secure API specification itself**. Separate first-party UI evidence registered as `S102` now verifies configurable webhook endpoints plus distinct **Project Created**, **Project Updated** and **Project Deleted** event selection. It does not establish a webhook API endpoint, signature/authentication mechanism, retry/replay contract or certified receiver.

Project creation, tasks, comments, milestones, file uploads and signatures therefore remain separately unverified. The webhook lane is more precise: configuration/event identity is verified from UI evidence, while sender integrity and delivery semantics remain pending.

Current integration state:

```yaml
api_contract_verified: true
base_url_verified: true
auth_header_contract_verified: true
project_read_contract_verified: true
project_update_contract_verified: true
secure_runtime_deployed: true
secure_runtime_version: 2
secure_runtime_state: active_vault_capable_read_certification_blocked_on_exact_credential_copy
server_side_vault_bridge: verified
screenshot_transcription_read_attempt: http_401
screenshot_transcription_result: not_character_exact
production_credential_bound_to_agent_runtime: pending_exact_secure_copy
authenticated_read_test: blocked_pending_exact_secure_copy
authenticated_write_test: not_attempted_write_gate_closed
webhook_ui_contract_verified: true
webhook_project_events_verified: true
webhook_sender_integrity_verified: false
webhook_retry_replay_verified: false
webhook_receiver_certified: false
```

Until credential binding and live read/write tests pass, agents must not claim that a status update was posted directly to Collab Portal. GitHub/Mintlify remains the governed work record and Gmail remains the notification fallback.

See [Collab Portal Secure API Adapter](/developers/collab-portal-secure-api-adapter) for endpoint, security, rate-limit, testing and project-manager integration rules.

## Phase 3 implication

The Approval Service and DAIL should accept signed/approved artifact references from the Collab Portal while keeping protected document contents in the appropriate restricted store. The future project-status adapter should use the same identity, evidence, least-privilege, idempotency and audit controls rather than becoming an unaudited side channel.