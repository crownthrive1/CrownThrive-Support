---
title: "Engine, Domain and Vendor Registry"
sidebarTitle: "Engines & Domains"
description: "The institutional inventory model for software engines, domains, deployments, vendors, licenses, renewals, and replaceable implementations."
icon: "boxes-stacked"
---

# Engine, Domain and Vendor Registry

CrownThrive's asset base includes a large number of purchased software products, hosted services, source-code licenses, plugins, APIs, domains, and vendor relationships. These are **operating inputs**, not the institution itself.

The August 2026 management registry records exactly **85 mapped engine/service rows and 82 domain rows** in its S100 source scope. Those are independently defined dated inventory universes, not interchangeable counts and not current-production totals. The exact source-row recoveries now live in the [Holdings 85-Row Engine & Service Seed](/knowledge/phase-2-99-workstream-3a-holdings-85-engine-service-source-seed) and [Holdings 82-Row Domain Seed](/knowledge/phase-2-99-workstream-3a-holdings-82-domain-source-seed). Later-discovered engines/providers/domains remain separate records rather than being forced into those source counts.

## Engine record

Each engine or external service should track:

- stable engine ID;
- vendor and product name;
- CrownThrive assignment(s);
- current implementation role;
- license type and purchase evidence;
- version/build;
- support/renewal status and date;
- hosting/deployment location;
- source-code custody and modifications;
- data stored or processed;
- API/integration capability;
- export/backup method;
- security and privacy classification;
- replacement candidate and migration notes;
- current, legacy, planned, or retired status.

## Domain record

Every domain should track registrar, DNS authority, renewal date, auto-renew state, canonical platform mapping, redirects, email dependencies, TLS, deployment target, historical assignments, and sunset/disposition state.

## Vendor-independence doctrine

The CrownThrive service identity survives implementation changes. If one booking engine, community platform, ad server, link tool, flipbook system, or analytics vendor is replaced, the institutional platform ID remains stable and the ledger records the succession.

## Current-target version and marketplace evidence rule

For CodeCanyon/Envato, direct-vendor and other third-party engines, the newest provider-published release is recorded as the **current target/reference version** when the provider evidence is authoritative enough to identify it. That is not an installed-version claim.

The registry keeps at least four independently queryable states:

- historical CrownThrive purchase/license evidence;
- currently installed/deployed version, only from CrownThrive production evidence;
- latest provider-published target/reference version;
- staging/migration/reconciliation state between the two.

Current marketplace prices are channel-specific acquisition evidence. They never overwrite historical CrownThrive purchase price. A sale price and normal price can coexist as dated marketplace observations. Support expiration is tracked separately from software operation, update entitlement and license validity; one is never inferred from another without current vendor terms.

The standard upgrade sequence is **stage → backup → diff → compatibility/test → reconcile customizations/data/API/license → governed promotion**. Until the sequence closes, the deployed version stays unchanged in the current-state registry even if the provider has a newer release.

### Reward Loyalty / CrownRewards boundary

Reward Loyalty is the self-hosted engine beneath CrownRewards / My CrownRewards. It is not Partnero / Crown Affiliates and it is not AdLuxe Network. Current CrownThrive production engine state remains **Reward Loyalty v4**. The current provider target/reference is **v5.27.0**, but `production_v5_deployed=false` until CrownThrive deployment evidence proves otherwise. The v4 installation remains intact while v5 staging, PHP 8.4.1\+ compatibility, REST/Agent API controls, least-privilege credentials, rate/idempotency/reconciliation and bounded read certification are prepared.

## Commercial-license review

Purchased script or SaaS licenses require review before CrownThrive exposes functionality commercially to end users. The registry should retain evidence and current vendor terms rather than assuming an old purchase permits every future business model.

## Continuity

Critical source, configuration, content, data, and documentation require independent backup/export. No vendor's dashboard is the sole system of institutional memory.