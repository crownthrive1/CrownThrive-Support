---
title: "Phase 2.99 — Institutional Memory & Asset Steward Seed"
sidebarTitle: "Memory & Asset Steward Seed"
description: "August 20, 2026 public-safe checkpoint for the non-voting institutional-memory, source/master lineage, and custody-control agent packet."
icon: "box-archive"
hidden: true
noindex: true
keywords: ["Phase 2.99", "institutional memory", "asset steward", "source masters", "custody", "Google Drive", "THIVEBASE"]
---

# Phase 2.99 — Institutional Memory & Asset Steward Seed

**Checkpoint date:** August 20, 2026

**Disposition:** public-safe candidate templates; not installed or activated

**Phase effect:** Phase 2 / 2.99 remains current; Phase 3 remains blocked

**Provider mutation:** none

## Added

This packet prepares:

- an uninstalled GitHub custom-agent template for ct.agent.institutional-memory-asset-steward, outside recognized provider paths;
- an uninstalled repository-skill template for source/master, product-asset, custody, and continuity work;
- a standalone fail-closed GitHub Actions validation workflow using exact Node-24-compatible action SHAs;
- a hidden and noindex Mintlify operating contract;
- a public-safe machine manifest;
- a logical institutional asset-custody JSON Schema;
- a deterministic, standard-library validator with adversarial self-tests;
- this append-oriented checkpoint.

## Authority boundary

The steward is non-voting and defaults to A1 preparation with D1 risk. It may prepare reversible public-safe records. D2 remains subject to independent verification, applicable specialists, A/B/C/D/S quorum, mandatory Agent D, unchanged green controls, and rollback. D3 remains reserved to an authorized human.

The agent template disables user and model invocation; both templates permit only future read/search tools. The skill template's `x-crownthrive-*` annotations record governance intent rather than provider enforcement. Neither template is under `.github/agents/` or the recognized `.github/skills/`, `.claude/skills/`, and `.agents/skills/` roots, so this packet does not install a recognized provider profile or skill. Programmatic invocation is unauthorized. Installation, invocation, or tool changes require a separate exact-head activation change.

No schedule, provider write capability, product activation, checkout, entitlement, rights grant, credential action, merge, production Mintlify publication, or phase advancement is authorized by this packet. A prospective execution-disabled THIVEBASE binding exists as external history; it is not activation and must be retained disabled or retired through governed D1 reconciliation if this candidate is abandoned.

## Public/private separation

The public packet contains stable identities, state models, logical schemas, evidence rules, validation, and safe operating doctrine.

It excludes:

- credentials, tokens, private keys, passwords, and signed URLs;
- private Drive identifiers and Supabase object paths;
- private DDL, RLS implementation, provider project identifiers, and restricted operational evidence;
- private contracts, customer records, privileged communications, and unrestricted rights evidence;
- unreleased master contents and confidential source filenames;
- proprietary scoring or economic calibration.

Private runtime implementation must remain in governed private custody. GitHub and Mintlify receive only the accepted public-safe projection.

## Collision and navigation disposition

The eight paths are additive and did not exist on the inspected canonical main baseline or the active Virality documentation branch. The two MDX pages are hidden and noindex. This packet deliberately does not edit docs.json, AGENTS.md, the shared agent registry, the control-plane pages, the hourly relay, the Phase 3 readiness gate, the API adapter matrix, or the canonical governed-merge workflow.

The candidate assigns `ct.relay.agent-a` as an operational routing/orchestration parent only, without vote or approval control. THIVEBASE runtime-parent readback is verified; canonical public inventory insertion and ordered collision reconciliation remain pending. The steward does not replace the Documentation Sentinel, Documentation Steward, Platform Registry Agent, Evidence Auditor, Rights and Governance Agent, Publishing Agent, or Chief-of-Staff / Orchestrator. Public inventory and these collision boundaries must be verified before any activation.

After branch and pull-request ordering is resolved, Agent O or the designated documentation integrator may prepare a separate navigation and registry reconciliation packet. Documentation existence alone does not authorize that integration.

## Validation contract

The validator must fail when:

- the steward becomes vote-eligible;
- D3 or provider writes become permitted;
- required files or identities diverge;
- the MDX pages lose hidden or noindex status;
- actions use mutable or non-SHA references;
- the workflow stops running self-test or packet validation;
- either template appears under a recognized GitHub agent or skill path, including a renamed path;
- agent invocation, programmatic authority, or tools expand beyond the inert read/search ceiling;
- the operational parent gains vote/approval control or parent/inventory activation gates are removed;
- role-collision or prospective-runtime-binding rollback terms drift;
- the standalone packet differs from its exact eight-file machine inventory;
- either workflow path block differs from the exact eight packet paths plus the two recognized-path sentinels;
- a public file contains credential-like material;
- a record claims dual verification without independent Drive, THIVEBASE registry, and private binary-parity evidence;
- docs.json or public Supabase migrations are added to this bounded packet.

## Next governed work

1. Independently review the public-versus-restricted IP classification.
2. Reconcile the packet against the latest canonical main and all active agent/custody branches.
3. Bind the canonical parent and inventory entry and accept the explicit role-collision boundary.
4. Run the unchanged documentation, security, action-runtime, and governed-merge controls.
5. Derive the applicable Security, AI/ML/LLM TEVV, Operations/SRE, IP/Rights/Licensing, and other specialist gates from the exact diff.
6. Obtain exact-head sovereign review with mandatory Agent D and no deny or block.
7. Only then prepare a separate exact-head change that installs the templates under recognized paths and deliberately grants invocation and least-privilege tools.
8. Design the private THIVEBASE tables, RLS, storage, queue, retention, backup, and provider-adapter implementation in its separately controlled boundary.
9. Connect assets only through read-before-write, immutable digest, readback, correction, rollback, and rights/canon/commerce separation.

No step may convert missing evidence into PASS or advance Phase 3.
