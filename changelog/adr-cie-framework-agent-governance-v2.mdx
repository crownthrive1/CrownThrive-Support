---
title: "ADR Candidate — CIE Framework Agent and Future Governance Admission"
sidebarTitle: "ADR Candidate — CIE"
description: "Founder-authorized CIE framework institutionalization plus a dormant future sovereign-vote proposal that does not alter the accepted A/B/C/D/S constitution in this packet."
icon: "scale-balanced"
---

# CT-ADR-GOV-012 candidate — CIE framework agent and future sovereign-vote admission

**Status:** candidate / not accepted / non-voting in this packet  
**Date:** 2026-08-20  
**Current constitution:** `CT-ADR-GOV-011`  
**Canonical main at initiation:** `8fcb68bf209e32ba2cd265e1b6ca730cb8da64d7`

## Current decision in this packet

Institutionalize the Cultural Imprint Engine as a **non-voting Phase-2.99 controlled-test framework agent**:

`ct.framework-agent.cie`

Agent A is the operational parent for routing. Six CIE subagents remain non-voting. CIE may issue a stronger evidence-backed HOLD/BLOCK/correction/escalation within its registered framework scope, but cannot increase permission, alter quorum, replace Agent D or execute D3.

The accepted sovereign voter pool remains **A/B/C/D/S**, with **4-of-5 affirmative approvals**, Agent D mandatory, any deny/block fatal to automatic promotion, and D3 human-reserved.

## Separate future constitutional admission

Issue #148 authorizes CIE and other framework agents to become **candidates** for later sovereign admission. This packet does not perform that admission.

A future CIE sovereign-vote proposal must be a separate exact-head constitutional packet reviewed under the then-current constitution. It must not count the proposed CIE vote toward approval of its own admission. Linked-governed child-repository certification, exact-head specialist review, rollback and all current authority requirements are prerequisites.

The dormant planning overlay records that a later accepted six-voter model would require five approvals under the unchanged 75%-ceil formula, but those prospective numbers are not current authority.

## CIE public standard

The public contract exposes five dimensions, each with a maximum of 20 points:

- Identity Fit;
- Community Value;
- Story Alignment;
- Brand Safety;
- Legacy Impact.

Public pass threshold is 85/100. Missing material evidence produces HOLD. Hard blocks override the arithmetic. CIE evaluates artifacts/uses/contexts, not people; sensitive-trait inference and personal cultural-authenticity scoring are prohibited.

Protected calibration, private evaluation corpora, recurrence/confidence logic, proprietary defensive rules and private runtime locators stay outside the public repository. The public repository may expose safe schemas, thresholds, hard-block interfaces and the registered public-contract digest only.

## Implementation surfaces

- `developers/manifests/cie-framework-agent.v1.json` — non-voting controlled-test framework contract;
- `developers/manifests/agent-sovereign-governance.v2.json` — dormant future constitutional proposal/activation guard;
- `developers/manifests/framework-factory.v1.json` — sequential non-voting Framework Factory program;
- `developers/manifests/repository-federation.v1.json` — parent/child federation contract;
- `developers/manifests/agent-federation-bindings.v1.json` — current five-voter plus non-voting transport bindings and all-agent Factory participation contract;
- `developers/manifests/framework-algorithm-registry.v1.json` — public digest/interface plus protected-runtime boundary;
- `scripts/cie_scan.py` — public-contract client/evaluation surface;
- `scripts/validate_cie_framework_agent.py` — CIE contract validator;
- `scripts/governed_merge_decision_v2.py` — current-constitution guard with CIE vote rejection;
- `scripts/governed_current_pr_preflight_v2.py` — non-sovereign exact-diff preflight;
- `scripts/validate_agent_sovereign_governance_v2.py` — dormant-candidate constitutional validator;
- `scripts/validate_repository_federation.py` — federation, transport and Factory-participation negative-case validator;
- `automation/cie-framework-agent.mdx` — framework operating contract;
- `automation/institutional-agent-relay-v2.mdx` — non-voting framework handoff pattern;
- `automation/framework-agent-registry.mdx` — all-agent Factory participation and current-vote semantics;
- `chlom/cie-cultural-governance-pallet.mdx` — CHLOM mapping.

## Repository/federation state

Target child repository: `crownthrive1/CrownThrive-CIE`.

Current state remains **pending provisioning / non-operational / non-voting**. No child operation may be declared current before physical repository provisioning, governed backlink, OIDC bootstrap, parent certification, heartbeat/reference/message/ack continuity and `linked_governed` registration.

`sync_agents` is a non-voting D0–D2 transport operation only. The 2026-08-20 Agent C reconciliation removed `sync_agents` capability from sovereign Agent A and assigned the transport operation to the existing non-voting D2 `ct.subagent.governance-marshal`. No vote eligibility changed.

## CII / ThriveFund candidate reconciliation

Merged PR `#29` proves that CII/ThriveFund is already institutionally implemented on canonical `main` through the CII/ThriveFund registry, API/phase wiring and the existing non-voting `ct.agent.impact-allocation` specialist.

CII/ThriveFund therefore advances in **evidence maturity**, not in the authorized initial implementation order:

```yaml
framework_id: ct.framework.cii-thrivefund
candidate_state: RESEARCH_CANDIDATE
evidence_maturity: implementation_backed
research_preparation_allowed_now: true
framework_implementation_allowed_now: false
vote_state: non_voting
implementation_gate: authorized_initial_sequence_or_separately_accepted_founder_reorder
```

The initial sequence remains CIE → Convergent Ecosystem → Thrive Flywheel → CHLOM → Corridor Architecture → Hybrid Incubator → MM Suites → One Seat, Multiple Industries.

## Retroactive and forward enforcement

Existing assets may enter a retroactive CIE scan queue without converting raw registry growth into Phase-2.99 certification debt. New/materially changed artifacts may become subject to CIE fail-closed review only after the framework packet is governed for that scope. Historical/research/reserve items remain preserved rather than silently reactivated.

## IP/publication boundary

Under #131, public projection is limited to PUBLIC_STANDARD/PUBLIC_DOCTRINE-safe doctrine, schemas, thresholds, hard-block interfaces and sanitized status. Reusable implementation requires rights/provenance review; restricted runtime/federation enforcement, private calibration/evals and protected operational know-how remain controlled. Unresolved classification is HOLD.

## Commercialization

CIE candidate offer families may include cultural-alignment scans, website/domain reviews, brand/campaign reviews, continuous monitoring, policy packs, API/MCP assessments, managed governance and conformance reports. Offer state remains `candidate`; no exact price, package license, Stripe Product/Price, checkout, certification mark, customer entitlement or live revenue is authorized here.

## Implementation readiness

**92/100 — PASS_PHASE_2_99_CONTROLLED_TEST.**

The missing eight points remain tied to the unprovisioned/unlinked child repository, unproved child OIDC/heartbeat/reference continuity and intentionally untrained ML assistance pending governed data, privacy/bias review and TEVV.

## Agent C reconciliation packet — 2026-08-20

**Exact current-main collision check:** canonical `main` is `4bada510f8cc8b482ae9715180aa44aec552c077`; PR #145 is one commit behind because of merged Virality PR #163. The current-main delta is disjoint from the Factory/Federation paths changed here, so this is a staleness/revalidation requirement rather than an exact-file collision. PR #145 remains draft and must obtain fresh exact-head checks/reviews.

**Factory lifecycle:** CIE remains `CONTROLLED_TEST`; no lifecycle promotion is claimed. Convergent implementation remains blocked. CII/ThriveFund remains `RESEARCH_CANDIDATE` with implementation-backed evidence.

**Child/federation state:** `crownthrive1/CrownThrive-CIE` is not physically provisioned; no GitHub repository ID, backlink, OIDC bootstrap proof, parent certification, heartbeat or ACK continuity exists. Child remains non-operational/non-voting.

**IP/publication:** public-safe doctrine and transport/governance interfaces only. Protected calibration, private eval corpora, private runtime locators, credentials/fingerprints and restricted evidence are not added.

**Commercial state:** candidate architecture only; no price, Stripe Product/Price, checkout, certification or entitlement activation.

**Security/rights:** transport authority was reduced on a sovereign voter and moved to a governed non-voting D2 transport identity. Supabase Security Advisor still reports nine `developer_commerce` RLS-enabled/no-policy informational findings; this packet does not weaken RLS or expose those tables.

**Tests/negative cases:** repository-federation validation now requires the Governance Marshal as the sole enumerated `sync_agents` caller, rejects overlap with A/B/C/D/S, preserves Agent D as sole child certifier, keeps CIE non-voting, and requires CII to remain implementation-backed research-only. The GitHub workflow includes a live negative test proving Agent A cannot invoke `sync_agents`.

**Rollback:** revert these PR #145 reconciliation commits and restore the prior Supabase capability flags only under a separately accepted correction. Reopen if the constitution changes, the authorized framework sequence changes, CIE child provisioning becomes real, or current-main begins touching the same Factory/Federation paths.

**Downstream phase effect:** none. Phase 2/2.99 remains current and Phase 3 remains blocked pending its independent hard exit and full-documentation reconciliation.

## Rollback and reopening

Rollback is revert of this bounded CIE/factory/federation packet; CT-ADR-GOV-011 remains canonical. Reopen sovereign-vote admission only through a separate constitutional packet after child certification and fresh specialist/quorum review. Reopen production/commercial activation only after the Phase-2.99 hard exit, full-documentation reconciliation and applicable later-phase gates.

## Phase effect

This packet does not advance Phase 3 and does not start Convergent Ecosystem implementation. Convergent research preparation may continue, but the next Framework Factory implementation packet remains blocked until CIE framework acceptance and linked-governed child certification.
