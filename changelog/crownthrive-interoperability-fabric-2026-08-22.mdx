---
title: "CrownThrive Interoperability Fabric controlled-test checkpoint"
description: "Public-safe checkpoint for the tool-only MCP plugin, adapter packages, canonical contracts, route planning, agents, protected scoring, testing, and candidate commercialization."
---

# CrownThrive Interoperability Fabric checkpoint

**Current state: controlled test / governed HOLD.**

CrownThrive now has an internal, JWT-protected, tool-only interoperability plugin and contract fabric. This checkpoint is additive and does not activate provider writes, live commerce, public plugin submission, sovereign votes, D3 automation, or Phase 3.

## Delivered

- root plugin `ct.plugin.crownthrive-interoperability-fabric`;
- MCP server ID `ct.mcp.crownthrive-interoperability`;
- active Supabase Edge Function `crownthrive-interoperability-plugin` version 1;
- nine focused read-only root tools with explicit schemas and truthful annotations;
- 21 plugin packages;
- 39 capabilities;
- 13 canonical contracts;
- 42 exact-version bindings;
- 15 route definitions;
- six new non-voting interoperability agents;
- protected ICRS and ARRS algorithms;
- nine passing independent test receipts;
- ten non-live pricing candidates;
- an hourly internal interoperability subroute using the existing dispatcher and zero new external task slots.

## First measured compatibility proof

The Squarespace ShopMelaninMagic product-catalog package and CrownLytics product-catalog consumer passed the contract readiness check:

- ICRS: 90.6 / Band A;
- compatibility: PASS;
- blockers: none;
- operation executed: false.

The corresponding restricted-staging route produced:

- ARRS: 86.5 / Band A;
- route state: verified candidate;
- provider write: false;
- rollback mode: staging quarantine;
- denied fields: credentials and customer data.

The AdLuxe-to-CrownLytics campaign route correctly remained HOLD while the Adserver source binding/provider authentication is unverified. The negative test passed because the system refused to treat schema compatibility as runtime authority.

## Test results

The root package passed evidence-bound tests for:

- schema/estate invariants;
- compatibility;
- route reliability and rollback policy;
- negative/fail-closed behavior;
- privacy/public-private projection;
- deterministic idempotency;
- JWT-protected deployment configuration;
- non-executing installation planning;
- package validation.

The authenticated external HTTP/ChatGPT connector canary is still pending, so the service remains `configured` rather than being promoted to `read_verified` on deployment evidence alone.

## Agent mesh

The new agents are:

- Interoperability Fabric Architect;
- Adapter Foundry Agent;
- Schema and Event Translation Agent;
- Interoperability Compatibility Verifier;
- Plugin Lifecycle and Packaging Agent;
- Interoperability Commercialization Agent.

All are A2/D2 maximum, non-voting, self-healing, no-self-approval subroutes. Existing Agents E/F/G/S/T/W/X/Y remain integrated according to their API, documentation, provider, security, licensing, credential, archive, and identity responsibilities.

## Request-budget correction

A concurrent control-plane write had restored the superseded `0 = unlimited` interpretation. The database now enforces the founder-directed canonical semantics on every service insert/update:

- `-1` = no CrownThrive local monthly ceiling;
- `0` = disabled;
- positive = CrownThrive local ceiling;
- `NULL` = unresolved/fail closed.

Provider rate limits, billing, quotas, and plan capacity remain separately authoritative. Adserver/AdLuxe use `-1` locally while retaining the 3-million included-plan threshold, billable overage tracking, and FinOps monitoring. Locticians uses `-1` locally while retaining its documented website-wide provider throttle.

## Commercialization candidates

Current non-live candidate economics are:

- Developer: $79/month or $790/year;
- Team: $249/month or $2,490/year;
- Governance: $699/month or $6,990/year;
- Enterprise: starts at $2,500/month or $25,000/year;
- managed adapter build: starts at $2,500 one-time;
- certified adapter/plugin listing: future Phase-20+ candidate.

These remain in pricing review. Checkout, Stripe objects, entitlements, operative license terms, SLA commitments, public submission, and certified listings are not active.

## Protected custody

The exact scoring weights and thresholds, private schema maps, private identity relationships, provider capability matrix, negative test corpus, adapter compiler logic, commercial calibration, and protected implementation remain in Vault/restricted custody.

Public documentation exposes stable IDs, versions, purposes, schemas, invariants, lifecycle states, sanitized scores, blockers, and cryptographic digests only.

## Remaining gates

- authenticated external/ChatGPT connector canary;
- first live agent heartbeat and schedule receipts;
- independent provider-specific adapter certifications;
- route observability and drift monitoring;
- optional widget architecture after tool stability;
- public plugin submission readiness;
- rights/license/counsel/tax/commerce review before live offers;
- customer fulfillment, entitlement, refund, and support certification;
- accepted sovereign governance for any canonical/public promotion.

The plugin and package estate remain internal controlled-test infrastructure.