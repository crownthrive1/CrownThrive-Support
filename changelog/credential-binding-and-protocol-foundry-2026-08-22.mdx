---
title: "Credential Binding & Protocol Foundry — 2026-08-22"
sidebarTitle: "Credential & Protocol Foundry"
description: "Public-safe institutionalization record for CrownLytics encrypted binding, credential-continuity skills, Vault-first proprietary protocols, continuous foundry agents and commercial candidates."
icon: "key"
tag: "Controlled Test"
keywords: ["CrownLytics", "credential binding", "protocol foundry", "Supabase Vault", "CHLOM", "agents", "2026-08-22"]
---

# Credential Binding & Protocol Foundry — 2026-08-22

## CrownLytics credential binding

The existing CrownLytics provider credential completed the founder-authorized one-time encrypted-envelope workflow. Primary and continuity-recovery Vault references are present and server-side equality is verified. The recovery reference contains the same provider credential and is **not** independently issued recovery.

The binding transaction also created an opaque immutable version archive, active-version record, protected custody records, credential-version event and restricted DAIL receipt. No raw credential was returned, committed or projected into public documentation.

Credential custody and provider authentication were certified separately. Vault-backed bounded `GET /user` and `GET /websites` both returned HTTP 200 application/json through the governed adapter; only status, schema/size and digest receipts were retained. General provider writes remain closed.

## Binding infrastructure

The controlled-test runtime now includes:

- transactional initial-binding RPC;
- founder/admin-authenticated one-time RSA-OAEP envelope creation and binding;
- a five-minute session/token boundary;
- client-side encryption and trusted ephemeral server decryption;
- server-side HMAC equality verification;
- immutable credential-version and rollback records;
- automatic continuity refresh;
- sanitized evidence only.

The prior MCP input surface that accepted raw credential material is disabled. The active CHLOM Vault Broker exposes only `chlom.credential.envelope.create` and `chlom.credential.envelope.bind` for credential entry. The temporary one-use bootstrap caller was retired after successful binding.

## Founder Directive v3 budget normalization

The service registry was reconciled to:

```yaml
0: unlimited CrownThrive local monthly ceiling
positive: explicit local ceiling
null: unresolved and fail-closed
-1: legacy compatibility only; never newly assigned
provider_limits_billing_quotas: separately_authoritative
```

## Proprietary protocol family

Five Vault-first controlled-test protocols were registered with protected custody, public-contract digests, owner/verifier separation and restricted DAIL evidence:

1. Credential Binding and Continuity;
2. Protected Asset Custody and Archive Continuity;
3. CHLOM Metaprotocol Protocol Foundry;
4. Protocol Commercialization and Licensing Continuity;
5. Provider Adapter Certification.

The private bodies remain non-public. Registration does not grant production activation.

## Agents, skills and schedules

Three non-voting/no-self-approval skill packages are registered:

- Credential Binding Continuity;
- Protocol Foundry;
- Protocol Commercialization Continuity.

The protocol-foundry and commercialization agents have provisional `did:chlom:` identities. Private fingerprint records and subject mappings remain private.

Two internal condition-watch subroutes were added without a new external scheduler slot:

- hourly credential binding, alias, version, custody and budget-semantic integrity;
- daily continuous protocol foundry.

The existing monthly pricing route now consumes protocol commercial candidates while keeping checkout and payment-object creation disabled.

## Commercial candidates

Research/HOLD records now exist for:

- Credential Continuity Audit;
- Secure Provider Binding Implementation;
- Metaprotocol Protocol Foundry License;
- Provider Adapter Certification;
- Protected Asset Archive Continuity Service.

No binding price, checkout, Stripe object or entitlement was activated.

## Remaining gates

- exact bounded contracts for additional CrownLytics read families;
- operation-specific provider-write certification only where separately authorized;
- independent D2 verification of proprietary protocols;
- next immutable encrypted archive revision and verified Drive readback after custody growth;
- D3 activation, binding economics and destructive changes remain human-reserved.