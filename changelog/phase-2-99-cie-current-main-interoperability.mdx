---
title: "Phase 2.99 — CIE current-main interoperability"
description: "Draft-only CIE agent-machine, parent/child interoperability, CHLOM pallet, retroactive scan, and next-framework continuity."
---

# Phase 2.99 — CIE current-main interoperability

**State:** draft / controlled test / not operational.  
**Canonical main observed:** `c7f14b73cff09f00a8f94f15a8587289de18ff7b`.  
**Framework:** `ct.framework.cultural-imprint-engine`.  
**Agent machine:** `ct.agent-suite.cie-interoperability`, `PRECERT_CONTRACT_VALIDATION_ONLY`.

This packet re-anchors CIE interoperability on current `main` without importing stale or private runtime topology from predecessor PRs #145/#169. It carries a public-safe envelope contract, a six-identity non-voting agent suite, a protected-runtime request/receipt schema, a sanitized CIE→CHLOM pallet and record contract, a plan-only retroactive scan contract, and a research-only CIE→Convergent Ecosystem handoff.

The child remains `PROVISIONED_UNLINKED`, non-voting, runtime-disabled and pending Agent-D exact-head parent certification. Exact private child head evidence remains in the governed private federation receipt and is intentionally excluded from the public packet.

Delegated children cannot independently verify Agent-C-authored work, certify the parent, create sovereign authority, call `sync_agents`, exercise D3, write CHLOM, execute a scan, dispatch the protected runtime, or activate Convergent Ecosystem.

Protected calibration, weights, private evaluation corpora, defensive rules, private runtime topology, credentials, fingerprints and restricted evidence are excluded from public and customer outputs. Missing protected runtime or digest evidence remains HOLD.

Commercial surfaces remain candidates only: reference pack, managed runtime, API/MCP integration, CHLOM pallet integration, Convergent readiness assessment, operator training, implementation service and conformance assessment. No exact price, Stripe object, checkout, payment link, license grant, certification, customer entitlement or automatic fulfillment is active.

The public validator verifies exact contract digests, non-voting boundaries, D3 reservation, person/profile rejection, no-execution scan state, CHLOM no-write state, protected-runtime HOLD, Convergent research lock, public IP exclusions and all commerce switches false.

Reopen this packet if canonical main moves, the governed private child head/digest moves, an exact contract digest changes, OIDC/identity evidence changes, the protected runtime becomes unavailable, #131 classification changes, a specialist denies/blocks, or an exact-head workflow/negative control fails.

No Phase 2.99 hard gate closes from this packet alone. Phase 3 remains blocked and Convergent Ecosystem remains research-only.
