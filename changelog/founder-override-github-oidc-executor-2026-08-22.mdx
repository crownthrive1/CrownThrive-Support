---
title: "Founder-authorized GitHub OIDC executor repair"
description: "Public-safe checkpoint for the bounded GitHub Actions-to-THIVEBASE execution bridge and first measured packet continuation."
---

# Founder-authorized GitHub OIDC executor repair

**State:** controlled test / governed hold.  
**Founder authorization:** typed-name institutional attestation from Kavonte Jones Sr. on 2026-08-22.  
**Authority effect:** bounded D0–D2 execution and evidence transport only. No D3, merge, sovereign-vote, payment, provider-write, credential-disclosure or Phase-3 authority.

## Diagnosis

The scheduled agents were waking and receiving packet-first prompts, but the schedule runtime did not have a reliable connected mutation path into the GitHub/THIVEBASE packet lifecycle. Stale detection, owner assignment and WIP selection therefore worked while continuing work remained parked.

## Repair

The controlled-test repair adds:

- a GitHub Actions OIDC bridge using short-lived provider-issued identity rather than a stored GitHub credential;
- stable repository/repository-ID/owner-ID checks;
- exact PR-number, live branch and exact-head verification;
- exact workflow-path and workflow-body SHA-256 binding;
- append-only forced-RLS executor bindings and receipts;
- bounded work-unit/progress ceilings;
- owner/verifier separation and no sovereign-vote effect;
- idempotency by workflow run, attempt and step;
- DAIL lineage and protected Vault custody for the private implementation;
- long-lived-PR historical-base fetching in the governed merge gate;
- suppression of redundant push-plus-pull-request execution receipts.

GitHub permits organization-level customization of the OIDC subject. The bridge therefore relies on the signed stable repository identity claims plus exact workflow/ref/PR/head checks rather than assuming one hard-coded subject-string template.

## First measured result

PR #220 moved from:

`running / 20% / four blockers`

to:

`verifying / 70% / two blockers`

The owner-side disclosure-classification and rollback/dry-run blocker groups were closed. Independent review and sovereign quorum remain. The executor receipt has no secret exposure and no sovereign-vote effect.

The first all-green checkpoint was `4e746fdbd522559dc2cfdb6820a5adc91391d9cb`. The reconciled current PR #220 head is `fdf8e6d10101dad950449e43ecc10b46d6cd2220`. At that exact current head, Documentation Governance, Security Governance, the Gen-7 Packet Executor and Governed Merge Gate all pass.

## Integrity hardening

The final DAIL verification exposed one legacy v1.0 event-hash construction defect. Its payload SHA-256 and physical previous-link hash were intact. The original event row and stored chain hash were **not** rewritten or deleted.

The repair adds:

- versioned DAIL hash construction;
- canonical UTC timestamp encoding for new v1.1 events;
- an append-only forced-RLS integrity-correction ledger;
- verifier support for an explicitly documented legacy correction while retaining physical stored-hash continuity.

After the correction and a new watchdog event, the chain verified **200 events with zero uncorrected failures and one documented legacy correction**. The integrity state is `pass_with_documented_legacy_correction`, not a false claim that the legacy defect never existed.

The protected executor protocol is registered as a restricted institutional asset and the updated protected estate was sealed into the next immutable August archive revision. Prior revisions remain preserved.

## Boundary

Technical CI and an executor receipt do not constitute sovereign acceptance. The packet remains draft/HOLD and requires independent verification plus the separately governed A/B/C/D/S path where applicable. History remains append-only; rollback uses ordinary revert commits and never force-pushes or silently deletes predecessor evidence.
