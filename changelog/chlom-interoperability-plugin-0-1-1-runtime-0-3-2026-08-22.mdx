---
title: "Interoperability Plugin 0.1.1 and Runtime 0.3 checkpoint"
description: "Public-safe version checkpoint for the MCP/App resource runtime, gap scanner, plugin package history and commercialization holds."
---

# Interoperability Plugin 0.1.1 and Runtime 0.3

The CrownThrive Interoperability Fabric has advanced through explicit versioning rather than silent overwrite.

## Version chain

- Plugin package `0.1.0` remains preserved.
- Plugin package `0.1.1` is the current controlled-test package.
- Runtime `0.2.0` remains preserved.
- Runtime `0.3.0` is the current controlled-test MCP/App runtime.
- Submission packet `v0.1` remains preserved and superseded.
- Submission packet `v0.2` remains preserved and superseded.
- Submission packet `v0.3` is the current non-operative candidate.

## Runtime 0.3 additions

- MCP `resources/list` support;
- MCP `resources/read` support;
- public-safe `ui://crownthrive/interoperability-dashboard.html` widget resource;
- tool annotations and output-template bindings;
- `interop.gaps.scan` deduplicated project-routing tool;
- versioned resource manifest;
- plugin and adapter version ledgers;
- research-only unit-economics models;
- database/RPC negative-test receipts.

## Current evidence

- the `interop.status` contract has independent certification evidence;
- the canonical THIVEBASE-to-CHLOM mapping has independent certification evidence;
- the THIVEBASE-to-CHLOM read route has independent certification evidence;
- the plugin has been independently returned for additional runtime/widget evidence rather than falsely approved.

## Current holds

The following remain explicitly open:

- authenticated deployed Edge/MCP readback;
- deployed Edge leakage tests;
- widget keyboard, screen-reader, zoom/reflow and mobile-device evidence;
- tenant isolation;
- remaining contract, mapping, route and adapter certification;
- rights/public-policy review;
- exact pricing and unit-economics evidence;
- fulfillment, entitlement and support canaries;
- governed submission and published readback.

Installation, submission, publication, checkout, entitlements and seller payouts remain disabled. Technical compatibility does not create provider-write, D3, sovereign-vote or merge authority.
