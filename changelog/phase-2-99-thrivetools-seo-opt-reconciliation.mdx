---
title: "Phase 2.99 — ThriveTools SEO/OPT Child-Capability Reconciliation"
sidebarTitle: "2.99 ThriveTools SEO/OPT"
description: "Founder-authorized Phase 2.99 reconciliation resolving ThriveTools SEO and ThriveTools OPT as child capabilities of the ThriveTools parent and crosswalking their subdomains and OPT API evidence without altering historical source counts."
icon: "sitemap"
keywords: ["Phase 2.99", "ThriveTools", "ThriveTools SEO", "ThriveTools OPT", "subdomains", "domain manifest", "API", "reconciliation"]
---

# Phase 2.99 — ThriveTools SEO/OPT Child-Capability Reconciliation

## Decision

Founder authorization on August 21, 2026 resolves the outstanding ThriveTools parent/child classification:

```yaml
parent:
  id: ct.platform.thrivetools
  name: ThriveTools
children:
  - id: ct.capability.thrivetools.seo
    name: ThriveTools SEO
    relationship: capability_of
    host: https://seo.thrivetools.io
  - id: ct.capability.thrivetools.opt
    name: ThriveTools OPT
    relationship: capability_of
    host: https://optimize.thrivetools.io
counting_rule: no_new_top_level_platforms
```

The decision preserves the recovered source labels while establishing the current institutional graph.

## Evidence crosswalk

The recovered platform/domain source names `ThriveTools`, `ThriveTools SEO`, and `ThriveTools OPT` as distinct labels. The current master-link manifest records `thrivetools.io` among primary domains and both `seo.thrivetools.io` and `optimize.thrivetools.io` among subdomains.

The source-count rule is therefore:

- preserve `S100-DOM-057` as the root-domain row in the historical 82-row S100 universe;
- attach the two recovered subdomains as child-host edges from a separate source manifest;
- do not mutate the 82-row historical source census merely because later source material contains additional subdomains;
- do not count SEO or OPT as separate top-level CrownThrive platforms.

## OPT API evidence

Current Drive evidence records ThriveTools OPT v4.0.0 with REST base `https://optimize.thrivetools.io/api` and a fail-closed API-control posture. API documentation, primary server-side credential binding and authenticated `GET /check-status` are recorded as passed; upload/delete writes remain closed. Remaining bounded-read, request-body, recovery/continuity and MCP exposure gates remain open.

## SEO boundary

The SEO child identity and subdomain relationship are resolved. Current SEO runtime/API/provider/deployment certification remains open because the domain manifest alone does not prove those states.

## Downstream reconciliation

This decision must propagate through:

- Holdings 68-row identity reconciliation;
- S103 stable identity and S100 relationship crosswalks;
- engine/domain identity edge register;
- domain and API discovery records;
- Platform API and Adapter Matrix;
- Current-State Validation Queue;
- Phase 2.99 plan;
- Phase 3 readiness gate;
- ThriveTools institutional registry.

## Gate

```yaml
identity_ambiguity: closed
subdomain_lineage: closed_source_backed
seo_runtime_verification: open
opt_runtime_verification: partial_open
phase_2_99_hard_exit: unchanged
phase_3_entry: blocked_pending_phase_2_99_hard_exit
```