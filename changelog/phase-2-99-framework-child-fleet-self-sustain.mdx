---
title: "Phase 2.99 — Framework Package Fleet & GitHub Self-Sustain"
sidebarTitle: "Framework Package Fleet"
description: "Correction checkpoint for one-at-a-time framework execution packages, package-native automation, OIDC isolation, Supabase package registry and commercialization-ready public-safe surfaces."
hidden: true
noindex: true
---

# Phase 2.99 — Framework package fleet and GitHub self-sustain

**Date:** 2026-08-20  
**Authority:** issue `#148`  
**Stack:** PR `#145` CIE Framework Factory packet + PR `#169` package-factory correction  
**Risk:** D2 workflow/federation/package architecture  
**Phase:** 2 / 2.99; Phase 3 remains blocked

## Founder architecture clarification

The packet distinguishes two states. An independently executable framework package can exist and perform bounded, non-voting controlled-test work in the canonical source host without a standalone repository. A `linked_governed` child is a stricter certification state and requires a physical repository with immutable GitHub repository ID, backlink, OIDC binding, exact contract digests, inherited governance/security, passing CI/evals, bidirectional message/ACK/reference/heartbeat proof and rollback/recovery evidence.

Physical-repository absence therefore does not erase package R&D, but it remains a hard blocker for Agent-D parent certification, operational activation, sequence advancement and any later sovereign-voter proposal.

## Current truth preserved

- Canonical `main` at correction start: `4bada510f8cc8b482ae9715180aa44aec552c077`.
- Active CIE parent packet: PR `#145`, exact head `79a00dc4641b0084cb78791a3c5e6132a4dae621` before this stacked correction.
- Current constitution remains `CT-ADR-GOV-011`; A/B/C/D/S only, Agent D mandatory, D3 human-reserved.
- CIE framework agent remains non-voting.
- CII/ThriveFund remains implementation-backed `RESEARCH_CANDIDATE`, outside the initial eight-package sequence.
- Phase 3 remains blocked by Phase 2.99 hard exit and the full-documentation hard gate.

## Package fleet correction

`developers/manifests/framework-child-fleet.v1.json` moved to package-model v2 and now declares:

- framework package is the primary child identity;
- physical repository is not required for package existence or bounded controlled test;
- a physical linked child is required for Agent-D parent certification and operational/sovereign activation;
- canonical source host is `CrownThrive-Support`;
- optional standalone repositories remain allowed only as distribution projections until separately linked and certified;
- package implementation remains one-at-a-time;
- pull-request validation receives no OIDC authority;
- OIDC mutation requires trusted runtime context plus workflow/ref/environment → agent/capability binding;
- public package distribution requires #131 IP classification;
- prices, checkout and entitlement remain separately authorized.

## Package contents

The Factory now renders:

```text
README.md
AGENTS.md
SECURITY.md
.crownthrive/federation.json
framework/manifest.json
engine/engine.py
skills/framework/SKILL.md
tools/tools.v1.json
api/api-contract.v1.json
mcp/mcp-tools.v1.json
evals/evals.v1.json
commercial/offer-manifest.v1.json
.github/CODEOWNERS
.github/dependabot.yml
.github/pull_request_template.md
.github/workflows/framework-child-bootstrap.yml
.github/workflows/framework-child-governance.yml
scripts/federation_client.py
scripts/validate_child_repository.py
```

The public engine is executable but fail-closed for protected decision logic. It returns `HOLD_PRIVATE_RUNTIME_REQUIRED` when a material operation requires CrownThrive's private calibration/runtime rather than copying restricted implementation into the distributable package.

## OIDC hardening

The prior child governance template granted `id-token: write` at workflow scope while also running on `pull_request`. That is corrected.

Current template policy:

- PR validation job: `contents: read` only;
- trusted non-PR federation job: `contents: read` + `id-token: write`;
- no provider/customer write;
- no direct-to-main repair;
- no auto-merge;
- no `sync_agents`;
- no child self-certification;
- no sovereign vote creation.

This closes the package-template OIDC-scope defect itself. The broader federation gate for exact workflow/ref/environment → institutional-agent/capability enforcement remains independently governed until the runtime proves it.

## Supabase / THIVEBASE correction

Migration `framework_package_registry_v1` created restricted table:

`institutional_federation.framework_package_registry`

It records the eight initial framework packages separately from optional repository projections.

Readback after migration:

```text
8 framework packages
CIE = controlled_test
7 later frameworks = source_discovery
physical_repository_required_for_package_existence = false for all 8
linked_governed_child_repository_state = UNPROVISIONED for all 8
parent_certification_state = PENDING_PHYSICAL_CHILD for all 8
operationally_enabled = false for all 8
public_activation_allowed = false for all 8
can_vote = false for all 8
Agent D certification state = pending for all 8
CIE private runtime state = registered
CIE API state = candidate
CIE MCP state = disabled
CIE commercial state = candidate
```

`algorithm_registry` now carries `implementation_package_id` for CIE. Historical `repository_registry` rows remain preserved as optional distribution projections for package-only R&D. They cannot satisfy the separate linked-governed child certification gate without immutable repository identity and the complete Agent-D evidence chain.

## One-at-a-time state

Current implementation package:

`ct.framework-package.cie` → Cultural Imprint Engine

Current state: `controlled_test`, non-voting, parent certification pending, public activation false.

Convergent Ecosystem remains source discovery with scaffold-preview permission only. Material implementation remains blocked until the CIE package satisfies its accepted predecessor gate.

## IP and commercialization

The package factory is explicitly designed to support public use and monetization while preserving the permanent IP gate.

Candidate surfaces include:

- licensed reference package;
- managed framework runtime;
- API/MCP access;
- implementation/migration service;
- operator/developer training;
- conformance assessment.

No exact price, Stripe Product/Price, checkout, license grant, certification status or customer entitlement was activated by this correction.

Protected calibration, private eval corpora, restricted evidence, credentials and trade-secret implementation remain outside public package outputs.

## Tests / negative cases

The package validator and parent fleet workflow now require:

- exactly eight ordered framework packages;
- CIE as the only current materialization packet;
- Convergent preview-only;
- physical repository not required for package existence/controlled test;
- physical linked child required for parent certification and operational/sovereign activation;
- unprovisioned packages cannot invent repository IDs or certification;
- required package files present;
- engine compiles and health runs;
- protected runtime absence produces HOLD;
- non-voting and D3 locks;
- Agent D remains sole certifier;
- PR validation has no OIDC;
- OIDC exists only in trusted federation runtime jobs;
- MCP tools remain disabled before governed promotion;
- no non-D0 tool is enabled before certification;
- commercial fail-closed fields remain false;
- credential-shaped values are rejected;
- pinned GitHub Actions references are preserved.

## Rollback / reopen

Rollback is a straight revert of the package-factory correction plus disablement of any package runtime binding. Historical repository planning rows are preserved rather than deleted.

Reopen on package identity change, workflow permission broadening, runtime/digest change, Agent D certification change, API/MCP schema change, IP/right classification change, predecessor acceptance change, distribution-license decision or constitutional change.

## Downstream

This correction strengthens Framework Factory continuity and public/product packaging without opening Phase 3. It removes a false infrastructure dependency while preserving the real governance, runtime, security, IP and commercialization gates.
