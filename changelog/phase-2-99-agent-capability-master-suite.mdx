---
title: "Phase 2.99 Agent Capability Master Suite Addendum"
description: "Additive controlled-test record for the governed agent capability suite."
status: "HOLD"
version: "1.0.0"
lastUpdated: "2026-08-21"
---

## Additive change

This record adds a controlled-test capability suite without replacing the prior Phase 2.97 or 2.99 history. Any older baseline statement remains preserved until a separate governed reconciliation explicitly supersedes it.

Added:

- 26 nonsovereign, nonvoting subagent specifications;
- rigid, fluid, and hybrid operating-mode definitions;
- per-agent privilege, skill-package, identity, heartbeat-registration, and schedule records;
- 14 ThriveAlumni public-surface support mappings;
- deterministic framework compilation and independent test routing;
- ZIP/file and workflow supply-chain validators;
- approval-gated, additive retroactive internal-link tooling;
- pricing and skill-commercialization candidates held behind existing gates;
- independent human-recovery plus replaceable private-object-store dual-custody policy; and
- hard database protections against silent deletion and self-activation.

Hardening patch `1.0.1` additionally:

- rejects unsupported or flow-style workflow syntax instead of allowing it to bypass action-pin and permission checks;
- requires each applied link receipt to match an exact digest and reviewer identity supplied through a separate trusted control-plane channel; and
- compiles only a strict candidate-field allowlist with the supported schema version, a versioned CrownThrive identifier, typed false consequential flags, and bounded authority.

Not changed:

- the five sovereign A/B/C/D/S voters;
- Agent D’s mandatory approval;
- D3 human reservation;
- the eight-framework Factory sequence;
- Phase 3 hard-exit status;
- live Stripe, checkout, entitlement, or exact product-price authorization;
- Mintlify publication state; or
- current ThriveAlumni legal authority, rosters, charters, or vote evidence.

## Evidence quarantine

The retained recovery generation remains preserved as title-level evidence. Detached, incompatible generation evidence remains `HOLD / NEED_TO_DO` and cannot certify the retained generation. Controlled counts, identifiers, checksums, and lineage details remain outside public Git.

## Approval required

This branch must remain draft/HOLD until repository reconciliation, independent verification, private-control-plane security review, dual-custody restore testing, and the governed A/B/C/D/S process—with D mandatory—are complete.
