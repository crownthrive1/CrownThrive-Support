---
title: "Commercial Release Package Agent — 2026-08-22"
sidebarTitle: "Commercial Release Agent"
description: "Institutionalization record for the automated credit-only release-package agent serving CrownThrive Launch, Ready and Procure."
icon: "clock-rotate-left"
tag: "Controlled Test"
---

# Commercial Release Package Agent — August 22, 2026

CrownThrive reconciled the existing non-voting, fail-closed `ct.agent.commercial-release-packager` and `ct.agent-suite.commercial-release-factory.v1` with a current-main public contract and deterministic product-package builder.

## Implemented

- reconciled 30 packaged products and 90 asset-version records from THIVEBASE;
- generated deterministic release packages for all 30 products;
- corrected public gate topology to match THIVEBASE: 10 canonical all-product dimensions plus one Ready-only qualified-professional-review dimension;
- preserved six historical gate labels as aliases rather than double-counting them as independent gates;
- preserved credit-only commerce and disabled cash checkout;
- registered and documented all 18 existing factory worker/skill packages;
- created public-safe API/MCP, schema, CI, publication and rollback contracts;
- preserved the existing Commercial Sites Governance Relay and external scheduler capacity;
- prepared current-main replacement documentation for the stale Launch/Ready/Procure packet;
- recorded Wallet Phase B/Phase C and domain-chain convergence work without fabricating independent review.

## Current THIVEBASE controlled-test state

The hourly release factory is now `v3.0.1` and preserves technical evidence through append-only reconciliation receipts.

Across the 30-product cohort:

- required canonical gate rows: 310;
- canonical technical PASS rows: 90;
- canonical HOLD rows: 220;
- package-integrity PASS products: 30;
- synthetic credit checkout/webhook PASS products: 30;
- synthetic refund/dispute/rollback PASS products: 30;
- accepted products: 0;
- published products: 0;
- cash checkout enabled: 0;
- credit checkout enabled: 0.

Synthetic canaries do not touch customer balances or move money. Operative license acceptance, private fulfillment, accessibility/device review, DNS/TLS/route proof, provider public access, professional review and governed publication remain HOLD.

## Current destination evidence

Anonymous provider readback currently returns HTTP `401 Sign in required` for Launch, Ready and Procure. The preferred custom hostnames currently fail DNS resolution from the governed runtime. Historical deployment evidence is retained, but it does not override current readback.

## Gate-topology correction

An earlier public candidate incorrectly expanded compatibility aliases into 18 apparent release dimensions. The authoritative runtime uses 10 canonical all-product dimensions plus one Ready-only dimension. The correction is additive and preserves the alias names for historical continuity without creating duplicate authority or duplicate PASS paths.

## Not activated

No product price, tax conclusion, license grant, checkout, entitlement, DNS change, direct Sites provider publication, governance vote, wallet merge or Phase 3 advancement was created by this implementation.
